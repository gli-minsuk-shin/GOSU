# GOSU 0.58.20 — installed, Keychain startup wait

Same-batch and same-run paper identity screening, including 0.58.19's verified cross-account Mail
groups. See [Briefing contract](../BRIEFING_WORKSPACE.md) and [release procedure](../RELEASE_RUNBOOK.md).

Current inspection confirms installed 0.58.17 is responsive and running a Briefing collection.
Do not interrupt it or describe the previously uninstalled mail dedup as deployed.
Paper screening uses exact identity/version only, not titles; legacy unversioned distinct sources
and unverifiable mail remain separate. Existing historical records are not deleted.

Also admits unverified same-Message-ID legacy Mail candidates to the existing bounded read plan
once, records attempts and suppresses old display rows only after a verified group covers their
exact delivery identities. This does not bypass Mail scope or retroactively guess equality.

## Verification

- Final `pnpm check`: format, generated contracts, lint, typecheck, full workspace tests, root
  script checks and production builds passed. Desktop 2,380 passed / 8 environment skips;
  Briefing 647 passed; Model Lab 341 passed; UI 15 passed. Skips: MacTeX opt-in (5), Linux-only
  (1), live Claude (1), live Hermes (1).
- Focused recheck/paper/store regressions: 22 passed. Named Agent Runtime: 623 passed
  (184 Desktop, 438 Briefing, 1 Model Lab).
- Directory package, source/staged/installed deep-strict signatures and hashes passed;
  installed isolated startup passed. The unchanged visual layout had prior synthetic smoke.

## Installation and limits

- Installed `/Applications/GOSU.app` reports 0.58.20. Source was
  `apps/desktop/dist/mac-arm64/GOSU.app`, from the dirty working tree at
  `a4d5ef7effe0f863f799d646236f97b01898d94a`; no commit or push.
- Backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.20-20260911-185624.app`.
- `app.asar` SHA-256: `1a1820c504156e179c6cd827c6319a71b9b60443c00561f212663e569a3f2b12`.
- Quit via native GOSU menu completed; old process absence was verified before replacement.
  No force termination, database reset, grant change or deletion of old records occurred.
- Relaunch window inspection timed out. A read-only one-second process sample shows the main
  thread waiting in SecItemCopyMatching/SecKeychainItemCopyContent/SecurityServer decrypt.
  Interactive startup awaits resolution of the OS Keychain access wait; no password, ACL or
  security dialog was manipulated. Local ad-hoc package, not notarized.
- New installed UI and actual cross-account originals remain unverified. Rechecking occurs only
  within the next permitted normal collection and existing limits. Unknown/multipart/attachment
  equality remains separate; historical records are not retroactively deleted or title-merged.
