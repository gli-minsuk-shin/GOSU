# GOSU 0.58.89 installed; Claude login code entry

See [Claude Code login service](../../apps/desktop/src/main/claude-code-login.ts),
[0.58.88 record](0.58.88.md) and [release procedure](../RELEASE_RUNBOOK.md).
Cumulative dirty source (Git HEAD `303360b`); no commit/push.

Installed 0.58.88 user check: Claude Code launched without a terminal completes sign-in by showing
an Authentication code in the browser ("Paste this into Claude Code") and waiting for it on stdin.
0.58.88 had no place to enter it, so in-app login could not finish. The user cancelled that pending
login and quit GOSU normally before this update.

Change: while sign-in is pending, the Claude card shows a masked, autocomplete-off code field and
`코드 입력`. Main forwards a trimmed, whitespace-free code of at most 4,096 characters only to the
active `claude auth login` process stdin and never stores or logs it. Codes are rejected when no
login is active. The start message now tells the user to paste the browser code.

## Verification and installation

Focused: login service, connection UI, Claude adapter, add-on service and Settings files, 58 passed
(new code forwarding and code field regressions included). Full check passed on Node 26.5.0:
Desktop 2,613 / eight existing environment skips (MacTeX five, Linux-only one, live Claude/Hermes
one each); Briefing 917, Model Lab 431 and other packages 268 were turbo cache replays; root scripts
18\. Typecheck/lint/format/contracts and production builds passed. Named Runtime gate passed on
Node 26.5.0: 1,782 (8 + 17 + 159 + 824 + 563 + 211).

Packaged with existing local Electron 43.2.0 through electronDist. Runtime preparation and packaged
startup smoke passed before and after fixed certificate signing; strict app/helper checks passed.
Update continuity preservedIdentity=true, requiresOneTimeApproval=false.
Source: `apps/desktop/dist/mac-arm64/GOSU.app`; installed `/Applications/GOSU.app`, 0.58.89.
ASAR SHA-256: `fbfd1efd631549030abe42cc2a2adfbe79047d247ecd4e27eebb2ab9743ca280`.
Certificate: `093F07514506D9D61282F362792B33741C834264`.

User quit GOSU before backup; no forced quit and no pending login process. Data backup, 77 files /
8,873,216 bytes (`gosu.db` byte-identical):
`/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.89-YKDoSy`.
Previous binary (0.58.88):
`/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.89-2026-09-15T11-49-39Z.app`.
Staged copy matched version, signatures and ASAR hash before the swap; staging directory removed.

Installed checks: strict app/helper signatures, identical ASAR hash, bundled code-entry IPC present
and isolated installed startup smoke passed. The app was not relaunched by the assistant; the real
Settings code field and a completed browser sign-in through GOSU remain to be checked by the user.
