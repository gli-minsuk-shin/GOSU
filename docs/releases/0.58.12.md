# GOSU 0.58.12 — installed, user approval / live UI verification pending

Status: local directory package installed on 2026-09-11. Signed bundle and isolated startup
verified. Actual application UI verification is blocked pending macOS security approval.

Contract: [Briefing integration](../BRIEFING_WORKSPACE.md),
[release procedure](../RELEASE_RUNBOOK.md), [maintenance guide](../MAINTENANCE_GUIDE.md).

- Shared Calendar, To-do list and Briefing Lab entries above Projects.
- Search/notifications remain in the shortcut row; Tasks moves to the named personal group.
- App-owned Briefing backend and bundled UI; existing data retained, new client permissions
  require the existing settings approval flow. Standalone port conflicts fail closed.

## Verification

- `pnpm check` passed: format, generated contracts, lint, types, workspace tests, root script
  tests and production builds. Desktop 2,370 passed / 8 environment skips; Briefing 613 passed;
  Model Lab 341, Core 116, Contracts 43, UI 15, Integrations 19, Domain 14, Sync API 51.
  Root script tests: 12/12. Desktop skips are MacTeX opt-in 5, Linux-only 1, live Claude/Hermes 1 each.
- `pnpm test:agent-runtime`: 181 Desktop + 421 Briefing + 1 Model Lab = 603/603.
- Focused navigation/frame tests 20/20. Sidebar Electron visual smoke: 4/4 light/dark and
  narrow/wide variants; rendered screenshot inspected. Exact top ordering and icon-only
  Search/notifications plus named Calendar/To-do list/Briefing Lab verified in fixtures.
- Packaged startup passed for build and installed app. Code signature verification passed
  (`codesign --verify --deep --strict`). Local ad-hoc signature; no notarization claim.

## Artifacts and recovery

- Source version: apps/desktop/package.json 0.58.12; Git HEAD
  `a4d5ef7effe0f863f799d646236f97b01898d94a` with pre-existing dirty/untracked work preserved.
  No repository commit/push performed. Candidate includes accumulated 0.58.11 work.
- Build: `apps/desktop/dist/mac-arm64/GOSU.app`.
- Installed: `/Applications/GOSU.app`; Info.plist confirmed 0.58.12.
- Backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.12-20260911-130106.app`
  (previous installed 0.58.10). Binary remains recoverable; user DB/Briefing stores not moved/deleted.
- app.asar SHA-256 (build, staging and installed matched):
  `073d95ba4173669a6f5745a7b76ed4dcc16423f8157d3fe03d17021b4a8d32b3`.
- Stage `/Applications/.gosu-stage-0.58.12.xe8GgB` removed only after empty. Old GOSU was
  idle with no composer draft, exited through normal Quit before replacement. The idle standalone
  Briefing Preview was stopped to release port 4318; no stored data was reset.

## Remaining user step and verification limit

Follow-up on 2026-09-11: the main GOSU window is now accessible, showing v0.58.12 and the three
named global entries above Projects. The user is on Appearance settings; no active turn/draft
was visible. This resolves the earlier main-window inspection block, not all private-data grants.

Installed GOSU starts but does not expose its main window to UI inspection yet. A new SecurityAgent
process appeared; attempts to inspect GOSU timed out, and the computer-use tool refuses access to
SecurityAgent. No password or approval was entered, no ACL/security policy weakened. The user was
asked to complete any macOS keychain approval directly. After approval, verify the visible version
and open all three global destinations. This live check is not replaced by fixture/startup tests.

On first embedded use, saved server profile configuration is imported without copying authority.
The user must review/save settings to authorize the new GOSU client. Existing private records are
not automatically exposed, and browser-only layout/custom questions may need separate restoration.
