# GOSU 0.58.40 — installed; native UI verification blocked

2026-09-14. Adds [assistant-approved paper saving](../SHARED_PAPER_LIBRARY.md). Includes the
[general discovery candidate 0.58.39](0.58.39.md); follow the [release runbook](../RELEASE_RUNBOOK.md).
Installed app is now 0.58.40. Binary verification passed; native user-workspace UI is unverified.

The owned conversation plus the current affirmative reply authorize a scoped paper-save tool.
Public lookup identifies the discussed papers; the summary-role model produces verified five-section
records. No copying the chat analysis as a paper. The host returns actual receipts, refreshes the
library and retains save confirmation in the transcript. Permission is checked again before commit.
Public source failure, unavailable identity, model failure and storage failure remain explicit.

## Gates

- Focused approval/assistant/ingestion/library/source/UI: 72 passed. Additional final approval/history
  check: 46 passed. A strict optional-property typing issue found during revalidation was corrected.
- Final `pnpm check`: Briefing 761; Desktop 2,409 passed / 8 existing environment skips; Model Lab
  341, Contracts 44, Briefing Core 116, UI 15, Integrations 19, Domain 14, Sync API 51; root tests 18.
  Lint, typecheck, formatting, generated contracts and production builds passed.
  Desktop skips: five MacTeX opt-in, one Linux-only, live Claude and live Hermes opt-in.
- `pnpm test:agent-runtime`: 948 passed (30 + 418 + 499 + 1).
- No real LLM or user-paper save/delete was used for QA. Tests exercise real encrypted temporary
  library storage and mocked source/model/approval flows, not the screenshot's actual paper contents.
- Conversation records add an optional server-owned `hasOtherPendingActions` marker so ambiguous
  bare assent stays rejected after reopen. Old records still load. Older binaries with strict schemas
  may not read newly marked records; do not assume a binary rollback is a conversation-schema rollback.

Source is dirty at `a4d5ef7effe0f863f799d646236f97b01898d94a`; no commit/push. The previous running
app's native window discovery previously timed out. At 01:16 KST the installed 0.58.38 main process
was no longer running; it was not force-stopped by this task. Do not manipulate SecurityAgent.
Actual artifact hash, backup and installed verification follow.

## Installed artifact and backups

- Candidate and installed version: 0.58.40. Candidate `apps/desktop/dist/mac-arm64/GOSU.app`;
  target `/Applications/GOSU.app`.
- Candidate/staging/installed asar SHA-256:
  `af7eee0832c5611d7c5d4b709be620b4756a7008a0a3fd77182764fa085e6eba`.
- Fixed signer: `GOSU Local Code Signing`, fingerprint
  `093F07514506D9D61282F362792B33741C834264`. Strict app/helper verification passed; continuity
  returned `preservedIdentity=true`, `requiresOneTimeApproval=false`. No trust/ACL/TCC changes.
- Old 0.58.38 app moved to
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.40-20260914-final.app`.
- State backup:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.40-20260914`.
  SQLCipher DB 3,895,296 bytes and encrypted workspace 2,306,250 bytes. Both compared byte-identical
  before swap; no DB WAL/SHM; backup directory 0700, files 0600. User data/settings were not reset.
- Staged in `/Applications/.gosu-05840-Ib9AyD`; verified before swap. Removed only that empty staging
  directory. Main process absence checked before backup and swap; no forced termination was used.
- Directory packaging, strict installed signature and isolated installed startup passed. This smoke
  does not open the user's database/Keychain or prove a real chat save.

## Native verification limit

Two attempts to obtain the installed GOSU window timed out. Main process runs, but the Briefing port
was not listening during the check. This is consistent with the earlier startup/security-approval wait,
not proof of the exact prompt or cause. No SecurityAgent interaction or workaround was attempted.
The user must inspect any displayed macOS GOSU/Keychain prompt directly. Installed header, restored
conversation UI and real screenshot-paper saves remain unverified. Do not call those papers saved.

Logs under `/private/tmp`: `gosu-05840-focused.log`, `gosu-05840-approval.log`,
`gosu-05840-check-final-approved.log`, `gosu-05840-runtime-approved.log`,
`gosu-05840-package-final.log`, `gosu-05840-sign-final.log`. Markdown mirrors verified.
