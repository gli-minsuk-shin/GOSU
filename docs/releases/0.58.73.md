# GOSU 0.58.73 installed

Paper summaries keep their header AI question button and repeat the identical action next to the
collapse button at the end of expanded content. The shared disclosure covers live, historical and
library papers. Both actions use the existing exact routine/history/paper reference; clicking does
not collapse the paper or send a question automatically. Missing references do not gain an action.
The compact footer wraps on narrow screens and keeps an 8px gap.

See [paper library](../SHARED_PAPER_LIBRARY.md) and [release runbook](../RELEASE_RUNBOOK.md).
Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`, cumulative dirty tree; no commit/push.
Regression reproduced the missing bottom button before the fix. Focused tests: 24 passed.
Final `pnpm check`: 4,095 passed, 8 existing environment skips (MacTeX 5, Linux 1, live
Claude/Hermes 1 each); format/contracts/lint/typecheck/production build passed. The first whole
workspace run had unrelated SSH timing failures during a slow local run and was stopped; a fresh
run passed without changing timeouts or tests. Runtime gate: 1,465 passed. Packaging and isolated
packaged startup passed.

Previous version was 0.58.72. Its idle paper-library UI was inspected and normal menu Quit
succeeded. Existing data and settings are not migration targets. Pre-update protected backup:
`/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.73-YaVleT`.
71 files / 8,554,684 bytes were copied and compared byte-identically: DB, routing/approval/shortcut/
language/usage settings, Preferences/Local State, Model Lab, Local/Session Storage, full Briefing store.

Installed `/Applications/GOSU.app` 0.58.73 from `apps/desktop/dist/mac-arm64/GOSU.app`.
ASAR SHA-256: `bc390c20d07bc2ae15188600e8d6f30eb08dfe30c555e3a6002cda5bc3de7caa`.
Previous app backup:
`/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.73-20260914-1824.app`.
Fixed signer `093F07514506D9D61282F362792B33741C834264`; strict app/helper verification and continuity
passed (`preservedIdentity=true`, `requiresOneTimeApproval=false`). Staged/installed hashes match,
installed isolated startup passed, empty staging removed. No OS trust/Keychain ACL changes or force quit.
Initial native launch lookup timed out; the next lookup showed actual v0.58.73. A task-creation
dialog containing user data was active, so it was not dismissed or submitted for visual QA.
Installed version/startup is verified; installed paper-footer interaction remains unverified.
Header/footer presence, placement inside disclosure and identical reference dispatch were verified
by focused component tests. No question was sent or user task created during verification.
