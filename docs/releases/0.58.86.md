# GOSU 0.58.86 candidate

See [sidebar AI activity](../SIDEBAR_AI_ACTIVITY.md) and [release procedure](../RELEASE_RUNBOOK.md).
Cumulative dirty source; no commit/push. Adds small drawn/twinkling stars tied to task lifecycles,
safe frame scoping, completion acknowledgement and reduced-motion handling. No AI work is started
by viewing or clearing the marker. Existing icon geometry and stored user data are unchanged.

Synthetic real-render QA passed at 280/332px in light/dark and confirmed completion acknowledgement.
Focused state, frame validation, native call, sidebar and background generation-monitor coverage added.

## Verified candidate; installation awaits normal quit

Full check passed: 4,230 tests / eight existing environment skips (MacTeX five, Linux-only one,
live Claude/Hermes one each). UI 23, Model Lab 422, Briefing 917, Desktop 2,605, other packages
245 and root scripts 18. Runtime 1,753 passed (8 + 17 + 159 + 822 + 563 + 184).
Typecheck/lint/format/contracts and production builds passed. Regression coverage includes
concurrent work, failure/cancel, acknowledgement, trusted frame/source/workload checks, source-
scoped reload/reset, Critical Review routing, native project tasks and actual Model Copilot client
success/error events. Font changes still do not reload the embedded frame.

Packaged with the existing local Electron 43.2.0 distribution through electronDist, avoiding the
previous release's remote download failure. Native dependency preparation, packaged startup smoke,
fixed signing and strict app/helper checks passed. Update continuity preservedIdentity=true and
requiresOneTimeApproval=false.

Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, version 0.58.86.
ASAR SHA-256: `c547cce9848a9650c7ffc258a456606d829289ba93af01f48185df23f678e8c5`.
Fixed certificate: `093F07514506D9D61282F362792B33741C834264`.
Installed app remains 0.58.85; its process was still running and its Briefing listener had not
started after the preceding keychain wait. The user was asked to handle the OS prompt and quit
normally after AI work ends. No forced quit, 0.58.86 data backup, binary swap or installed UI
verification was performed. Take a fresh backup and verify current state before installation.
