# GOSU 0.58.53 candidate

Subsequently included and installed with [0.58.55](0.58.55.md); this candidate was not installed separately.

See [public paper recovery](../PUBLIC_PAPER_RECOVERY.md) and [release runbook](../RELEASE_RUNBOOK.md).

The screenshot was recurring Briefing collection, which still used only arXiv; the previous
multi-source recovery was chat-only. The default collector now uses independently bounded arXiv,
Crossref and OpenReview queries. A failed provider cannot discard valid alternatives. Existing
research filters and saved-summary exclusions are preserved. Empty failed History headings say
조회 미완료 instead of implying zero papers.

## Verification

- Focused discovery/default collector wiring/History tests: 38 passed.
- Briefing full suite: 795 passed. Agent Runtime: 1,275 passed (62 + 649 + 518 + 46).
- Live public-only smoke with a generic `neural networks` query and 30-day window: a real arXiv
  HTTP 429 was recorded, but OpenReview supplied one eligible article in 2,833 ms. The public record
  was `https://openreview.net/forum?id=uLOdtBm0Cx`, published 2026-09-01. This is metadata/abstract
  retrieval, not a PDF/full-paper verification or a guarantee that every research profile has new
  articles. Crossref completed without a transport failure, but no eligible record survived filtering.
- Production History component rendered in a synthetic browser fixture with the heading
  연구 논문 · 조회 미완료 and preserved failure explanation. No actual user settings, private Mail,
  saved library content or LLM calls were used in the search probes/visual fixture.
- Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`; local dirty changes include prior updates.
  No commit/push requested or performed in this update.

`pnpm check` passed: 3,976 tests (3,958 workspace + 18 root), 8 existing environment skips
(MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each). Formatting, generated contracts,
lint, typecheck and production builds passed.

## Artifact and installation boundary

Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, version 0.58.53.
`app.asar` SHA-256: `bdd884764e1ae1352ab71bd1e1d8ae963de8d7b68bf652b7eed9199e72a6239f`.
Directory packaging, fixed certificate signing, strict app/helper checks and isolated startup passed.
Continuity against `/Applications/GOSU.app` reports preservedIdentity=true,
requiresOneTimeApproval=false. No new certificate, Keychain ACL or public trust changes were made.

Installed version remains 0.58.52. Native AX and screenshot reads timed out repeatedly while the
installed process remained running. No force quit, binary replacement or data backup/swap was
performed. User was asked to quit normally before continuing the release runbook. Recheck process
absence and create fresh protected data and old-app backups before installation; no backup for 53
exists yet. Installed-53 UI and the user's actual live collection remain unverified.
