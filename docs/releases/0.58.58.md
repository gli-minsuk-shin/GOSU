# GOSU 0.58.58 installed

See the [weather contract](../BRIEFING_INTELLIGENCE.md#weather-chart-contract) and
[release runbook](../RELEASE_RUNBOOK.md).

The shared weather chart uses a fixed 0–100% probability axis. Only 0/50/100% axis ticks
remain; hourly probability labels and the visible dry-window caption are removed. Bars use
a proportional 64px scale in a consistent 210px chart. Hover/focus/selection retains details.
Forecast records, conversations, permissions and automatic briefing settings are untouched.

Focused weather tests: 8 passed. Briefing suite: 808 passed. Real browser rendering checked
the wide mixed-probability chart and narrow dry fixture. Both have only three probability axis
labels; selecting the 50% bar exposes its exact hourly detail. No source/LLM call was made.

`pnpm check`: 4,001 passed (3,983 workspace + 18 root), 8 existing environment skips
(MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each). Format, generated contracts,
lint, typecheck and production builds passed. The first attempt found a missing navigation
link in this release note; it was fixed and the complete gate rerun passed.
`pnpm test:agent-runtime`: 1,419 passed (74 + 772 + 525 + 48).

Source HEAD is
`303360b9b729d65bf568f79da3fbe1a137ccb67c` with existing local dirty work preserved.
No Git commit/push.

## Installation

Installed 2026-09-14 at `/Applications/GOSU.app`, from `apps/desktop/dist/mac-arm64/GOSU.app`.
Fixed-certificate signing and app/helper strict verification passed. Continuity reports
preservedIdentity=true/requiresOneTimeApproval=false; OS approval behavior is not guaranteed.
The app was idle with an empty composer, then normally quit through its native menu. Process
exit and absence of the DB WAL were checked before copying protected state.

- SHA-256 of app.asar: `88f2bac8e725e8ecb13c3a289431048399ef202aa9f5e0f7895ae6e25588b221`.
- Previous app: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.58-20260914-121322.app`.
- State backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.58-20260914-121322`.
  DB 3,936,256 bytes; workspace 2,436,042; memory 278,350; generation 1,422; routing 279;
  approval policy 59; usage ledger 5,728. Copies byte-identical, files 0600/directory 0700.
- Candidate/staging hash, strict signing and continuity passed; installed isolated startup passed.
  Empty staging was removed. Original data files were not replaced or reset.
- Actual installed UI shows v0.58.58 and the preserved 4시간마다 automatic interval. Initial
  cold-launch UI lookup timed out; subsequent native UI and backend listener checks succeeded.
  Chart geometry was visually checked in synthetic browser fixtures, not a new live-source run.
