# GOSU 0.58.11 — paper analysis save confirmation

Status: built and verified candidate; GOSU restart approval pending. Not installed or claimed released.

Implementation contract: [Shared paper-analysis library](../SHARED_PAPER_LIBRARY.md).
Deployment procedure: [Release runbook](../RELEASE_RUNBOOK.md).

- Source: dirty local workspace based on `a4d5ef7effe0f863f799d646236f97b01898d94a` (2026-08-30).
  Existing user changes are retained. No commit or push was requested/performed.
- Installed version before this work: 0.58.10, read from `/Applications/GOSU.app/Contents/Info.plist`.
- Change: GOSU Project Chat, embedded/standalone Model Lab and Briefing Chat offer to save completed
  paper analyses only after explicit user approval. Approved copies use a shared encrypted local
  paper-analysis library and appear in Briefing Lab's paper session. Source analysis is preserved
  verbatim rather than regenerated or passed off as newly read source evidence.
- Full `pnpm check` passed: format, generated-contract check, lint, typecheck, all workspace tests,
  root script tests and production builds. Affected full suites: Desktop **2368 passed / 8 skipped**,
  Model Lab **341/341**, Briefing **441/441**, contracts **43/43**. Desktop skips are the existing
  opt-in MacTeX tests (5), Linux-only filesystem test (1), and live Claude/Hermes opt-ins (2).
  No feature regression was skipped. Named Agent Runtime passed **179 Desktop + 257 Briefing +
  1 Model Lab = 437/437**. Maintenance navigation tests passed **2/2**.
- The initial root test run exposed an outdated exact-source assertion for the Model Lab message
  map; it now accepts the message index needed by the offer. A separate oversubscribed run timed
  out the existing 603-record test. Briefing's test script now caps workers at two; no test was
  removed and no timeout increased. Shared cross-app library/UI inputs are in Turbo global cache
  dependencies so changed shared files cannot leave a cached stale Desktop/Model Lab build.
- Packaged candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, Info.plist **0.58.11**, macOS arm64,
  development ad-hoc signature (not notarized). `codesign --verify --deep --strict` and the isolated
  packaged-startup verifier passed. Prepared Hermes **0.19.1** runtime verified during packaging.
- `app.asar` SHA-256: `d7ec0c53a2d47ad8a0578bc0c08937411159fe15f61a98465d9781e601554be4`.
- Existing `/Applications/GOSU.app` remains **0.58.10**. No app backup or replacement has occurred,
  because the user was asked asynchronously for restart approval and has not answered yet.
- Synthetic browser QA: paper response displayed the confirmation card without adding a record;
  the typed reply **넣어줘** changed library count 3→4 and showed a host-confirmed saved state.
  Existing routine summaries remained visible. Tests of encrypted cross-app persistence used
  temporary stores and injected keys, not user content or live Keychain writes. The new feature
  inside the installed GOSU binary is not yet verified because that binary was not replaced.
- Briefing Preview was restarted after confirming only its esbuild child and no active external
  connections. Assets: `index-4pJ_nPY2.js`, `index-OPpRtj5j.css`; stored settings/history were not reset.
- One native GOSU state inspection took about 20 minutes unexpectedly; later UI QA used the isolated
  browser with bounded calls. The observed installed app was on its task-list screen, with no
  open editor visible; hidden background work was not assumed safe to terminate without approval.
- No private paper, user conversation, account metadata or credentials are included in this record.
