# GOSU 0.58.55 installed

See [token efficiency/accounting](../TOKEN_EFFICIENCY_AND_ACCOUNTING.md) and
[release runbook](../RELEASE_RUNBOOK.md). Includes pending 0.58.53 paper recovery and 0.58.54 approved
scope reuse. No original conversation, memory or connection was reset.

Source now selects request-specific history, preserves full-history access and records native
AI Assistant/Briefing/Model Lab/compaction usage alongside existing per-project/model analytics.
Input totals no longer disappear when optional cache-write fields are missing.

## Verification

- Focused context/native/assistant tests: 54 passed; usage/ledger/Project Chat context/view: 43;
  Model Lab context/native: 19. Final Usage view follow-up: 12 passed.
- `pnpm check`: **3,994 passed** (3,976 workspace + 18 root), 8 existing environment skips:
  MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each.
  Formatting, generated contracts, lint, typecheck and production builds passed.
- `pnpm test:agent-runtime`: **1,406 passed** (66 + 772 + 520 + 48), including a final rerun.
- Synthetic 160-message Project Chat estimate: 321,235 → 10,100 input tokens (96.86% reduction),
  original records preserved. This is not an actual billed-cost or live-latency benchmark.
- Real-render synthetic Usage fixture verified workload summary, actual-model labels, per-project
  model rows, cache-read columns and readable scrollable tables. No private text or paid inference
  was used for these tests. Native usage lifecycle uses mocked receipts in regression tests.
- Git HEAD: `303360b9b729d65bf568f79da3fbe1a137ccb67c`; local dirty changes include previous updates.
  No commit/push requested or performed in this update.

## Installation

Installed 2026-09-14 after confirming GOSU was stopped and no DB WAL remained. No force quit.
Source: `apps/desktop/dist/mac-arm64/GOSU.app`; installed: `/Applications/GOSU.app`, version 0.58.55.
Candidate/staging/installed hash and strict signature verified; isolated startup passed. Fixed signer
was retained, with preservedIdentity=true/requiresOneTimeApproval=false. This is not a new public
notarization or guarantee of no future macOS permission prompts.

- `app.asar` SHA-256: `c1a37428f680ee4596844c72a31010c060beb05fd1af31de2b8f1c369e720caa`.
- Old 0.58.52 app: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.55-20260914-110501.app`.
- State backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.55-20260914-110501`.
  DB 3,936,256 bytes; encrypted workspace 2,368,678; encrypted memory 259,174; model-routing JSON 279.
  Files were byte-identical, 0600 in a 0700 directory. User data was not overwritten by installation.
- Staging hash/identity/version rechecked before swap; empty staging removed.
- Actual installed header v0.58.55 and existing Project Chat/AI Assistant history rendered. The initial
  cold-start native lookup timed out, then backend listener and subsequent lookup succeeded.
  Approval-policy preference initialized on disk. User changed the active screen during Usage-tab
  navigation, so installed Usage interaction was not forced/repeated; synthetic UI evidence remains
  distinct from a live new-model billing receipt. No test question or permission change was sent.
