# GOSU 0.58.65 installed

See [lightweight models and task export](../BRIEFING_TASKS_AND_LIGHTWEIGHT_MODELS.md) and
[release runbook](../RELEASE_RUNBOOK.md). Adds a third configurable lightweight role for calendar
drafting and explicit email → GOSU task/Apple Reminders creation, with scope checks and idempotency.

The native Calendar/Reminders helper compiled successfully for macOS arm64. Synthetic Spark/low
calendar drafting completed correctly in 4.748 seconds; no private source was sent and no event
was written. This is a single synthetic example, not a latency or cost guarantee.

## Verification

- Focused suites: Desktop 68 passed; Briefing 28 passed. Covers durable local IDs, lost local/native
  acknowledgements, exact source/payload deduplication, different-content/source preservation,
  archived/changed tasks, private scope revocation, native authorization separation, UI double clicks,
  trusted-frame refresh, old routing files and the independently routed lightweight job.
- `pnpm check`: 4,034 passed (4,016 workspace + 18 root), 8 existing environment skips
  (MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each). Desktop 2,573, Briefing 825,
  Model Lab 358. Format, contracts, lint, typecheck and production builds passed on final source.
- `pnpm test:agent-runtime`: 1,440 passed (77 + 782 + 533 + 48); new task regressions were added
  to the named gate. Initial development import/format/type errors and old factory-arity assertion
  were fixed before the full successful gates.
- Real browser fixtures showed the three model roles, adjacent Calendar/Task buttons and the reviewed
  task modal with project, date, notes and iCloud list selection. These fixtures use mock APIs.
- Native Reminders source compiled for arm64/macOS 14; no private task/reminder was created and no
  Reminders authorization prompt was invoked during verification. iPhone arrival and live two-system
  writes remain unverified until the user authorizes Reminders and submits an actual task.
- OpenAI Docs was consulted to distinguish a separate Spark model from higher-credit Fast mode.
  The local connected catalog and successful synthetic inference, not assumed account availability,
  determined this machine's selection. No Fast-mode setting changed.
- Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`, existing dirty work preserved; no commit/push.

## Installation and configuration

Installed `/Applications/GOSU.app` 0.58.65 from `apps/desktop/dist/mac-arm64/GOSU.app` after checking
idle/empty composer and normal GOSU menu Quit. Process exit/no DB WAL were checked before backups.
An old CUA app handle timed out; selecting the current app established its idle state before quitting.

- app.asar SHA-256: `be39d55c2edab5fdd06e790a4c26bafda3ee56092697ae686a3b8d8392ed85b7`.
- Previous app: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.65-20260914-144659.app`.
- Protected state: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.65-20260914-144659`.
  DB 3,960,832 bytes; workspace 2,550,950; memory 303,286; generation 1,106; routing 279;
  approval policy 59; usage ledger 10,911; shortcut 30; language 35. Task-links file absent.
  Copies byte-identical, 0600 files in 0700 directory; original data was not replaced.
- Candidate/staging/installed hashes, fixed-certificate app/helper strict signing, continuity
  (preservedIdentity=true/requiresOneTimeApproval=false) and installed isolated startup passed.
  Empty staging removed. This does not grant the new Reminders OS permission.
- After replacing the binary and while GOSU was stopped, the validated routing store added only
  Spark/low and lightweightTasks→lightweight. Existing fast/strong and all existing usage choices
  were checked unchanged. New routing file is 407 bytes. Older apps reject its extended fields:
  pair a binary rollback with the protected pre-update routing backup, never reset other settings.
- Actual UI reported v0.58.65. Cold lookup initially timed out; subsequent read succeeded. A later
  settings-navigation action was rejected because the window changed, so no further blind actions
  were sent. Stored model selection is verified; installed Reminders/phone end-to-end is not claimed.
  The automatic-generation file remained byte-identical after restart.
