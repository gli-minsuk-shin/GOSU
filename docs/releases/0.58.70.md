# GOSU 0.58.70 candidate

See [sidebar ordering](../PROJECT_SIDEBAR_ORDER.md) and [release runbook](../RELEASE_RUNBOOK.md).
Adds mouse drag reorder for active projects, insertion feedback and menu alternatives. Ordering
persists in existing sidebar state without changing project data, open sections or selection.
0.58.67–69 changes are included. Full release gates and installation pending.

## Verification

Focused regressions cover upward/downward insertion, unchanged project data and selection/expansion,
local-state roundtrip, legacy defaults, duplicate/deleted IDs, new projects, hidden state, cancellation,
external file drops, disabling/removing a drag source and keyboard menu controls. The language test
now expects a localized drag hint while preserving a project literally named Settings as user data.

Actual browser mouse drags moved Gamma before Alpha and after Beta. Reload retained the custom
ordering. A fast drop transitioning from text to row padding initially produced no native drop;
accepting dragenter as well as dragover fixed it, and both top/bottom padding drops then succeeded.
Temporary diagnostic event logging was removed. Only synthetic project IDs in the test origin were
reordered; the user's real project order has not been changed.

## Final gates

- Focused: 38 passed across ordering, sidebar, navigation storage, briefing subnavigation and
  language UI tests. The final fast-padding drop refinement is included.
- `pnpm check`: 4,072 passed (4,054 workspace + 18 root); Desktop 2,589, Briefing 841,
  Model Lab 364. Eight existing environment skips (MacTeX opt-in 5, Linux-only 1,
  live Claude/Hermes 1 each). Format/contracts/lint/typecheck/production builds passed.
- `pnpm test:agent-runtime`: 1,464 passed (80 + 792 + 538 + 54). Sidebar-order regressions
  were added to this named release gate.
- Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`, dirty changes preserved, no commit/push.

Installation is pending: the bundle identifier matches several app backups, so the explicit installed
path was used. The native computer-use pipe closed before returning current UI state. No normal quit
could be verified and no forced quit or app replacement was performed. The user's active work and
existing navigation storage remain untouched. Recheck state and take fresh backups after user quit.

## Ready artifact; not installed

Candidate `apps/desktop/dist/mac-arm64/GOSU.app` 0.58.70 has ASAR SHA-256
`e963c7ec4fd7bafab7c668c78232ba5fe97b43b6d8e3236196f1a2f8b98b3e22`.
Packaging, isolated startup, fixed-certificate strict app/helper signing and update continuity passed
(`preservedIdentity=true`, `requiresOneTimeApproval=false`). Final docs tests 2/2 and formatting passed;
Markdown mirrors are byte-identical. The browser fixture and preview server were closed.

Final installed version remains 0.58.66 with its main process running. No backup, staging or
replacement for 0.58.70 has been made. After normal quit, verify artifact/version again and back up
the current app, protected state/settings and user-data Local Storage before replacing. Preserve
that existing Local Storage directory rather than clearing it. Installed UI verification is pending.
