# GOSU 0.58.64 installed

See [Briefing workspace](../BRIEFING_WORKSPACE.md) and [release runbook](../RELEASE_RUNBOOK.md).

Moves Collapse All into the generation action row and gives the progress column a stable responsive
width. Expanded description text no longer controls the toolbar width. Retains native disclosure,
default collapsed progress, scheduling, action handlers and stored briefing content.

Focused generation/layout/progress tests: 15 passed. Synthetic browser checks at 1000px and 380px
container widths found identical x/y/width/height for Collapse All, interval select, Generate and
Stop before/after opening details. Narrow horizontal overflow was zero; screenshots were inspected.
No real sources or LLM generation were started for visual verification.

`pnpm check`: 4,018 passed (4,000 workspace + 18 root), 8 existing environment skips
(MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each). Briefing 819, Desktop 2,563,
Model Lab 358. Format, contracts, lint, typecheck and production builds passed.
`pnpm test:agent-runtime`: 1,424 passed (74 + 772 + 530 + 48).
Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c` with existing dirty work preserved;
no commit/push.

## Installation

Installed 2026-09-14 after checking enabled Generate/no running job and normally quitting GOSU.
Process exit and no DB WAL were verified before copying protected state. No force quit or
user-data replacement. Source `apps/desktop/dist/mac-arm64/GOSU.app`, target `/Applications/GOSU.app`.

- Version 0.58.64; app.asar SHA-256:
  `03f8a0de52eac496ef4b521c1ef9d8bf8e54fe6d304b72a4e7e7364e73990870`.
- Previous app: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.64-20260914-133408.app`.
- State: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.64-20260914-133408`.
  DB 3,936,256 bytes; workspace 2,543,578; memory 299,730; generation 1,154; routing 279;
  approval policy 59; usage ledger 8,585. Shortcut file absent. Copies byte-identical,
  0600 files in 0700 directory. Generation file remained byte-identical after restart.
- Candidate/staging/installed hashes, fixed-certificate app/helper strict signing and continuity
  passed (preservedIdentity=true/requiresOneTimeApproval=false). Installed isolated startup passed;
  empty staging removed. No OS security settings changed.
- Native UI shows v0.58.64 and retained Project Chat history. Cold launch initially timed out;
  subsequent UI checks succeeded. Navigation did not reliably reach the Briefing view, so no real
  generation was started to produce a progress panel. Expanded/collapsed geometry verification
  is the synthetic browser check above, not a claimed live-source/LLM or installed-job test.
