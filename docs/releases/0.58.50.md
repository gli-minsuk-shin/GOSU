# GOSU 0.58.50 candidate

Superseded by [0.58.51](0.58.51.md): the user rejected the smaller speech bubble. This candidate
was never installed. Its retained staging copy must not be used for the next replacement.

See [sidebar design](../SIDEBAR_ICON_DESIGN.md) and [release runbook](../RELEASE_RUNBOOK.md).

The AI assistant's four-point sparkle grows from 6×6 to 10.5×10.5 SVG units (1.75× per axis).
It uses the existing light/dark green accent and a slightly inset closed three-dot bubble, keeping
the 18px graphic, 22px slot, button geometry, navigation and conversation behavior unchanged.
No animation, image asset, new dependency or permission change is introduced.

## Verification

- Focused sidebar regression passed, including larger green sparkle geometry, retained closed
  three-dot bubble, decorative accessibility and the common slot.
- Native production-renderer visual smoke: **4/4 variants passed** (light/dark, 260/332px sidebars).
  Actual screenshots were inspected. Sparkle bounds are 10.5×10.5 and remain inside the 24-unit
  canvas; accent is distinct from the muted bubble. All icons remain 18px inside centered 22px
  slots, with no horizontal overflow and working navigation. The old visual fixture's Review
  ordering was aligned with the already active Critical Review menu.
- Full `pnpm check`: **3,954 passed / 8 existing environment skips**. Desktop 2,547, Model Lab 356,
  Briefing 773, Contracts 45, Briefing Core 116, UI 15, Integrations 19, Domain 14, Sync API 51,
  root scripts 18. Format, generated contracts, lint, typecheck and production builds passed.
- `pnpm test:agent-runtime`: **1,236 passed** (30 + 649 + 511 + 46).
- Skips: five MacTeX opt-in, one Linux-only, live Claude and live Hermes opt-in. No live LLM or
  research workflow was deliberately started for this icon change.

## Candidate and blocked installation

- Source base: `303360b9b729d65bf568f79da3fbe1a137ccb67c` plus this uncommitted icon/docs/test change.
  No new commit/push was requested in this icon task.
- Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, **0.58.50**.
- Asar SHA-256: `00d2d4d5a79cec61e6bdecbd3af93133d15bf3fac2fefe4d3bf203be58e5615f`.
- Packaging, isolated startup and fixed-certificate strict app/helper signing passed. Signer:
  GOSU Local Code Signing, `093F07514506D9D61282F362792B33741C834264`.
- Update continuity passed: `preservedIdentity=true`, `requiresOneTimeApproval=false`; this is not
  an OS-prompt guarantee. A staging copy at `/Applications/.gosu-update-0.58.50-Y8rxeX/GOSU.app`
  passed matching version/hash and strict-signature checks and is retained for the next attempt.
- Installed `/Applications/GOSU.app` is still **0.58.49**, process 47174. The installed old icon was
  visually inspected before the change. An idle/empty-input check preceded a normal Quit shortcut,
  but the app remained running. A menu attempt reported changed user window state. No forced quit,
  live DB copy, new backup or swap followed; its WAL remained present. Ask for normal user shutdown,
  then recheck process absence and stage identity before following the backup/replacement runbook.
- Installed 0.58.50 UI remains unverified. Conversations/settings were not reset; existing backups
  remain documented in [0.58.49](0.58.49.md).

Logs in `/private/tmp`: `gosu-sparkle-check.log`, `gosu-sparkle-runtime.log`,
`gosu-sparkle-visual.log`, `gosu-sparkle-package.log`, `gosu-sparkle-sign.log`.
