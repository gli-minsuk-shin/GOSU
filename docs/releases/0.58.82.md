# GOSU 0.58.82 candidate

Interest vote toggling in live and saved Briefing email/paper cards.
See [Briefing intelligence](../BRIEFING_INTELLIGENCE.md) and [release procedure](../RELEASE_RUNBOOK.md).
Cumulative dirty source at HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`; no commit/push.

Clicking the selected interested/not-interested thumb sends explicit decision null and clears both
selection and stored vote after success. Clicking the other thumb switches the vote. Pending requests
remain disabled/locked, and failures keep the last saved selection. A local null tombstone prevents
late restoration responses from restoring a cleared vote. Both API routes accept null; encrypted
memory removes only the exact routine/source vote. The feedback profile revision changes and future
ranking no longer includes that vote. Original emails, papers and historical summaries remain intact.
Repeated clear is idempotent and reselecting remains possible. Existing scope/confirmation guards remain.

Focused tests cover both thumbs, live/history parity, pending/failure behavior, encrypted restart,
profile neutrality, routine isolation and both API routes. Full gates and installation pending.

## Verified candidate, installation deferred

Full check passed: 4,185 tests / eight existing environment skips (MacTeX five, Linux one,
live Claude/Hermes one each). Runtime 1,626 passed (140 + 818 + 552 + 116).
Type/lint/format/contracts and production builds passed. Synthetic real-render QA exercised both
selected thumbs back to neutral, without changing user votes. Package startup, fixed signing and
continuity passed: preservedIdentity true, requiresOneTimeApproval false.

Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, version 0.58.82.
ASAR SHA-256: `8e98322ed6acd636fed94cb337c749ba4449b28e14d656ef73090192a924f2e6`.
The final installed-app check showed an active Model Copilot turn. No quit, backup, swap or user
feedback click was performed. Installed app remains 0.58.81. After work ends, obtain fresh idle
state and a fresh protected backup, reverify hashes/signing, and follow the normal replacement runbook.
