# GOSU 0.58.56 installed

See [Briefing workspace](../BRIEFING_WORKSPACE.md) and [release runbook](../RELEASE_RUNBOOK.md).

Automatic briefing interval remains a durable user preference. Startup/storage errors no longer
rewrite it to off. Harmless approved-client/model/UI changes do not invalidate scheduler authority;
actual permission/provider/account changes pause dispatch while retaining the selected interval.
Frontend shows loading/error rather than off before restored status arrives. Current installed
UI was observed at 4 hours; do not infer or reset another interval during installation.

## Verification

- Focused generation/UI tests: 30 passed, including restart/coalescing, approved-client append,
  exact legacy migration, harmless model changes, transient retry, real permission pause and explicit Off.
- `pnpm check`: 3,999 passed (3,981 workspace + 18 root), 8 existing environment skips:
  MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each. Format, generated contracts,
  lint, typecheck and production builds passed.
- `pnpm test:agent-runtime`: 1,411 passed (66 + 772 + 525 + 48).
- Actual browser rendering of production controls showed 불러오는 중… disabled while synthetic
  status was pending, then restored 4시간마다 without a schedule mutation or generation call.
- Current installed 0.58.55 UI also showed 4시간마다 before replacement. No real interval was
  changed, mail/calendar was queried or LLM was invoked for fixture verification.
- Git HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`, dirty local source including earlier changes.
  No commit/push in this update.

## Installation

Installed 2026-09-14 after idle-state inspection and native GOSU menu → Quit. Confirmed process exit
and no DB WAL before backup; no force quit. Source `apps/desktop/dist/mac-arm64/GOSU.app`, target
`/Applications/GOSU.app`, version 0.58.56. Candidate, staging and installed hash/strict signatures
passed, and installed isolated startup passed. Fixed identity retained; continuity reports
preservedIdentity=true/requiresOneTimeApproval=false, not a guarantee about future OS prompts.

- `app.asar` SHA-256: `eefd8f96b2dc0a632310e170adabb1da41fae7e6b65c374c47ab0ee25f76f93d`.
- Prior 0.58.55 app: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.56-20260914-112707.app`.
- Protected backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.56-20260914-112707`.
  DB 3,936,256 bytes; encrypted workspace 2,402,938; memory 271,406; generation settings 1,474;
  model routing 279; approval policy 59; usage ledger 1,717. All copies byte-identical, 0600 in 0700.
  User data files were not replaced. Empty staging removed after verified swap.
- After launch, generation settings remained byte-identical to their pre-update backup.
  Actual installed UI showed v0.58.56, 4시간마다 and its existing next execution time, along with
  retained Briefing History. No interval-change or manual-generation action was used for verification.
  Initial native lookup timed out during cold start; backend readiness and subsequent UI lookup passed.
  Actual future four-hour dispatch was not awaited; restart/coalescing dispatch uses regression tests.
