# GOSU 0.58.111 candidate

See [history view](../../apps/briefing-lab/src/briefing-history-view.tsx),
[0.58.110 record](0.58.110.md) and [release procedure](../RELEASE_RUNBOOK.md). Cumulative dirty
source; no commit/push.

## Standing usage paragraph removed from "저장된 AI 브리핑"

User request (with a screenshot): the paragraph under "저장된 AI 브리핑" — "최신 브리핑부터 아래로 스크롤해
읽으세요. 새 브리핑을 만들어도 이전 기록은 유지됩니다. 오늘·내일은 각 브리핑에 표시된 일정 날짜 기준입니다.
저장본 열람에는 원문 조회나 AI 호출이 없습니다." — is not needed.

Change: the paragraph is removed; the header keeps the title and the search box, and "삭제한 브리핑 보기",
the loading status and removal notices are unchanged.

Follow-up (user: "응 빼자") for the two other standing explanations:

- The task line under the agenda ("할 일은 … 최대 6개를 표시합니다 … To-do list에서 확인하세요") is removed; only
  an incomplete read keeps a short note ("일부 할 일만 조회했습니다. …").
- The routine email read notice ("이미 요약한 메일은 제외하고 전체 최대 N개를 조회합니다 … 건너뛰었습니다 …") is no
  longer written (`mailReadNotice` returns nothing and the mail result carries no `notice`), and runs
  saved earlier hide it. The first-connection and new-account notices stay, because they explain why an
  email section shows only up to three messages; failures and gaps keep their own alerts.

## Verification

The history view test now also checks that the paragraph is gone while saved history still renders;
a stored routine email notice and the standing task line are not rendered while an incomplete task read
still is; `mailReadNotice` is empty for a routine read and keeps the first-connection text; the mail
ingestion integration test expects no notice on a routine read (previously it required the skipped
count in that text).

Full check on Node 26.5.0 passed: format, contracts, lint, typecheck, test and build. Tests: Desktop
2,635 passed | 8 skipped, Briefing Lab 1,002, Model Lab 431, Briefing Core 116, sync-api 51, contracts
45, UI 23, integrations 19, domain 14 (4,336 package tests); root scripts 18. Named Runtime gate:
1,821 (8 + 17 + 163 + 835 + 587 + 211).

Packaged with the existing local Electron 43.2.0 through electronDist. The packaged startup smoke
passed before and after fixed certificate signing; strict app and CalendarBridge checks passed.
Update continuity preservedIdentity=true, requiresOneTimeApproval=false. The renderer bundle that
`index.html` loads no longer contains the removed paragraph and still contains the incomplete-task
note. Installed `/Applications/GOSU.app`, 0.58.111. ASAR SHA-256:
`55e1e7bd65dd051ae92efdeab6923e65c74c3f78de161c9d873e6e7344018a87`. Certificate:
`093F07514506D9D61282F362792B33741C834264`.

GOSU was closed (no process before staging or before the swap); no forced quit. Backup: 81 files /
11,514,942 bytes (`gosu.db`, `local-key.v2.json`, the whole `credentials/` tree,
`model-routing.v1.json` and `workspace.v1.enc.json` byte-identical):
`/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.111-20260918T071421Z`.
Previous binary (0.58.110):
`/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.111-20260918T071421Z.app`.
`model-routing.v1.json` has the same SHA-256 before and after the install. The staged copy matched
signature and ASAR hash before the swap; the staging directory was removed.

Installed checks: strict app and helper signatures, identical ASAR hash, no leftover staging
directories and an isolated installed startup smoke passed.

Found in passing: `apps/desktop/out/briefing-lab/assets` keeps every earlier renderer build (77
`index-*.js`, 115 MB), and all of them are packed into the ASAR; only the one `index.html` names is
loaded. Not changed in this release.
