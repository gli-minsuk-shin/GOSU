# GOSU 0.58.63 installed

See [icon design](../SIDEBAR_ICON_DESIGN.md) and [release runbook](../RELEASE_RUNBOOK.md).
Includes pending 0.58.59–62 changes.

Reduces only the assistant drawing from 28px to 18px, matching Search/Notifications. Button size,
22px alignment slot, bubble/sparkle artwork and navigation remain unchanged. Current installed
0.58.58 screenshot confirmed the oversized drawing before the edit.

Focused icon/sidebar regressions: 20 passed. Native synthetic visual smoke: 4/4 light/dark × 260/332px variants
passed; all three drawings measured 18×18px and zero alignment error. Light/dark screenshots
were directly inspected.

`pnpm check`: 4,016 passed (3,998 workspace + 18 root), 8 existing environment skips
(MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each). Desktop 2,563, Briefing 817,
Model Lab 358. Format, contracts, lint, typecheck and production builds passed.
The first full run found the prior sidebar test explicitly requiring 28px; its size expectation
was updated to 18px while preserving artwork/navigation assertions, then the full gate passed.
`pnpm test:agent-runtime`: 1,422 passed (74 + 772 + 528 + 48).

Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c` with existing dirty work preserved;
no commit/push.

## Installation

During work the installed app changed externally from 0.58.58 to 0.58.62. Fresh native UI checks
confirmed that generation had finished (Generate enabled, no Stop) and only saved content was
being displayed. No interruption approval was needed or inferred from the unanswered question.
Normal menu Quit completed and process exit/no DB WAL were checked before fresh backups.

- Installed `/Applications/GOSU.app` version 0.58.63 from `apps/desktop/dist/mac-arm64/GOSU.app`.
- app.asar SHA-256: `b5165b2dec26cefe74b78f5490204e825d59afb8ecbe0545322339a023a9e78c`.
- Previous 0.58.62 app: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.63-20260914-131704.app`.
- State backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.63-20260914-131704`.
  DB 3,936,256 bytes; workspace 2,540,962; memory 299,730; generation 1,154; routing 279;
  approval policy 59; usage ledger 8,016. Shortcut file absent (default); copied files were
  byte-identical with mode 0600 in a 0700 directory. User data was not replaced.
- Candidate/staging/installed hashes, fixed-certificate app/helper strict signing, fresh continuity
  against 0.58.62 (preservedIdentity=true/requiresOneTimeApproval=false), and installed isolated
  startup passed. Empty staging removed. No force quit or OS security-policy change.
- Actual installed screenshot shows v0.58.63, smaller balanced assistant icon and restored Project
  Chat history. Initial cold-start lookup timed out; subsequent UI and backend listener checks
  succeeded. The generation settings file remained byte-identical after startup.
- Prior 0.58.59–62 source fixes are included; this task did not claim installed live LLM/event-write
  or actual shortcut-keypress verification. Private screenshot content was not copied into docs.
