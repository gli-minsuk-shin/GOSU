# GOSU 0.58.16 — candidate

Includes the uninstalled 0.58.15 typography update, inline Briefing subnavigation and direct saved
connection reapproval. See [workspace contract](../BRIEFING_WORKSPACE.md) and
[release procedure](../RELEASE_RUNBOOK.md).

## Verification

- Final `pnpm check`: passed format, generated contracts, lint, typecheck, full workspace tests,
  root script checks and production builds. Desktop 2,378 passed / 8 environment skips;
  Briefing Lab 627 passed; Model Lab 341 passed; UI 15 passed; Briefing Core 116 passed.
  Skips are MacTeX opt-in (5), Linux-only (1), live Claude (1), live Hermes (1).
- Named Agent Runtime: 618 passed (184 Desktop, 433 Briefing, 1 Model Lab).
- Directory package, deep/strict code signature and isolated packaged startup passed for
  `apps/desktop/dist/mac-arm64/GOSU.app` (local ad-hoc signature, not notarized).
  `app.asar` SHA-256: `13337d9359b5dc8a66ec7ebd4debf60b8602faedef276da6f9396b126028743e`.
- Focused service/navigation tests: 36 Briefing and 3 Desktop passed; recovery/bootstrap 6 passed.
- Sidebar real-render visual smoke: 4/4 light/dark and 260/332px sidebar variants passed with
  three visible nested Briefing entries. Screenshot inspected. Initial smoke caught a label-flex
  selector stretching icon slots; scoping it to non-icon labels fixed it, then full gates reran.
- First root docs gate required another release navigation link; added release-runbook link and
  reran successfully. No failed gate is treated as passing.

## Installation boundary

User subsequently approved restart. Installed app remains 0.58.14: native window inspection and
normal Quit did not respond, so no running-app replacement or forced termination was performed.
The assistant's pending Quit request was cancelled. Candidate staging is
`/Applications/.gosu-stage-0.58.16.lmI5By/GOSU.app`; inspect current process state before continuing.
No native permission approval, account reconfiguration or private-source live test was performed.
The dirty working tree at `a4d5ef7effe0f863f799d646236f97b01898d94a` contains this candidate;
no commit/push. Installed UI and actual post-update permission recovery remain to be verified.
