# GOSU 0.58.57 installed

See [weather contract](../BRIEFING_INTELLIGENCE.md#weather-chart-contract) and
[release runbook](../RELEASE_RUNBOOK.md). Removes repeated zero-probability labels from the shared
Live/History chart. All-zero forecast windows use one label and a compact baseline band (158px chart
instead of 218px); all-missing probabilities stay distinct. Mixed charts combine only adjacent zero/
missing runs, preserving positive labels, proportional bars and per-hour hover/focus/click details.
Resize handling refits wet/dry transitions even when hour count is unchanged. Forecast data is untouched.

## Verification

- Focused weather tests: 8 passed, covering dry/missing grouping, positive labels/bars, historical
  and DST data, missing temperatures, and same-length wet→dry ResizeObserver refitting.
- `pnpm check`: 4,001 passed (3,983 workspace + 18 root), 8 existing environment skips:
  MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each. Format, generated contracts,
  lint, typecheck and production builds passed.
- `pnpm test:agent-runtime`: 1,419 passed (74 + 772 + 525 + 48).
- First full run timed out in the existing 603-history storage test while gates ran concurrently.
  Its isolated rerun (15 passed) and a complete rerun passed without weakening/changing that test.
- Real browser rendering: wide and 380px dry cards each showed one probability label, 158px chart
  height and no horizontal overflow. Mixed 0/1/5–100%/missing data retained positive labels/bars;
  selecting a zero marker showed the exact hour, temperature, apparent temperature and 0% detail.
  Fixtures made no weather API, private-source or LLM call and changed no user data/settings.
- Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`; local dirty updates include prior work.
  No Git commit/push in this task.

## Installation

Installed 2026-09-14 after confirming idle/empty composer and normal GOSU menu → Quit; no force quit.
Process exit and no DB WAL were confirmed before backup. Candidate/staging/installed version,
hash and strict signature checks passed; installed isolated startup passed. Fixed identity retained:
preservedIdentity=true/requiresOneTimeApproval=false, not a guarantee about future OS prompts.

- Source `apps/desktop/dist/mac-arm64/GOSU.app`; installed `/Applications/GOSU.app`, version 0.58.57.
- `app.asar` SHA-256: `db9fc4545505964e8c476efda3bcc7f5d6104b2963c9425f0db1704136925d56`.
- Old app: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.57-20260914-115440.app`.
- Protected state: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.57-20260914-115440`.
  DB 3,936,256 bytes; encrypted workspace 2,408,062; memory 271,406; generation 1,278; model routing
  279; approval policy 59; usage ledger 4,009. Copies byte-identical, 0600 files in 0700 directory.
  User data was not replaced; empty staging removed after the verified swap.
- Actual installed header v0.58.57, 4시간마다 control, and new all-zero SVG descriptions were observed.
  Cold-start lookup initially timed out; backend listener and later AX/screenshot reads succeeded.
  The native window became unavailable during a later scroll attempt, so it was not forced.
  Fine-grained chart layout was directly verified in the synthetic wide/narrow/mixed fixtures above.
  No new weather generation or LLM call was manually started for verification.
