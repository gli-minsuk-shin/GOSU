# GOSU 0.58.31 — full-size global assistant chat

Installed on 2026-09-13. See [release procedure](../RELEASE_RUNBOOK.md) and
[global assistant contract](../GLOBAL_ASSISTANT.md).

User clarification: retain the canvas changes and change only the assistant shortcut icon in
this follow-up. It now uses a complete rounded bubble with three dots. The refreshed candidate
has been rebuilt and verified; the hash below supersedes the earlier canvas-only artifact.

Dedicated global chat canvas replaces inherited sidebar geometry. The host gets an explicit
assistant viewport, and the retained Briefing pane uses explicit React presentation state without
remounting its conversation. Messages use readable left/right bubbles; the larger composer stays
at the bottom; suggestions form compact two-column cards. Standard Briefing sidebars are unchanged.
Provider, permissions, source access, stored history and inference behavior are unchanged.

Synthetic production-component browser QA inspected empty state and a long fixture-only response,
including log scrolling without moving the composer. Native installed-app screenshot inspection
returned an old project screen, so the user's actual initial assistant geometry was not reliably
captured; a screenshot was requested. No real message/source/model call was made for visual QA.

## Prepared candidate

- `pnpm check` passed: Desktop 2,394 passed / 8 existing environment skips; Briefing 698 passed;
  Model Lab 341 passed. Formatting, contracts, lint, typecheck and production builds passed.
- Agent Runtime 667 passed (197 Desktop + 469 Briefing + 1 Model Lab).
- Focused chat canvas/retention tests: 11 passed. They verify same-pane identity and draft retention
  when switching global/History modes, with no navigation-triggered inference.
- Follow-up icon/sidebar regressions: 19 passed. Production-component browser rendering confirmed
  the rounded three-dot bubble at the existing optical size; click behavior and canvas were retained.
- Directory package and isolated startup passed. Local certificate signing and strict app/helper
  verification passed; update continuity reported preserved identity/no one-time approval.
- Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, version 0.58.31.
- `app.asar` SHA-256: `5a2d4b28551aa6856efd7f26c4b472a68f10d9de8d67cf97e768a16935cd8690`.

## Installation

- GOSU was already stopped when replacement was requested; process absence was checked again
  immediately before moving the old app. No force kill or user-data reset.
- Previous 0.58.30 backup:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.31-20260913-175912.app`.
- Staging `/Applications/.gosu-stage-0.58.31.me8Cjz` was verified and its empty directory removed.
- Installed `/Applications/GOSU.app` version 0.58.31. Candidate, staging and installed asar hashes
  matched the hash above. Strict code-signing verification and installed isolated startup passed.
- Native launch succeeded after an initial inspection timeout. Accessibility and screenshot both
  showed v0.58.31; the actual screenshot showed the new three-dot assistant bubble and retained
  project/chat history. No live inference, source collection or account mutation was initiated.
- This is fixed local-certificate signing, not public Developer ID notarization. Settings, source
  connection files, Keychain/TCC and stored histories were not changed by the installer.
