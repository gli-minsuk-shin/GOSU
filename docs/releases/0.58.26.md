# GOSU 0.58.26 — role-based model settings

Follow the [release runbook](../RELEASE_RUNBOOK.md) and
[model routing contract](../SHARED_MODEL_CATALOG.md) before installation.

Candidate only; installed app is still 0.58.20. Includes fast/strong model and effort profiles,
workload routing, generation-relative New badges, exact Calendar links, and the bundled Calendar
helper from the preceding source work. Existing explicit model selections and permissions remain.

The user completed creation of a local code-signing identity. It is reported as not trusted by
the generic codesigning trust policy, but a synthetic executable signed with its exact fingerprint
passed strict signature/designated-requirement verification without changing any root trust.
`scripts/sign-local-mac.mjs` uses that explicit certificate instead of automatic Developer ID
discovery, retains strict cryptographic verification and checks both app/helper certificate pins.
It retains the existing local-development runtime/entitlements configuration; it is not a
hardened/notarized production release. No private key is exported; no Keychain ACL is modified.
This is local development signing, not Developer ID distribution or Apple notarization.

Release gates passed: `pnpm check` (format, contracts, lint, typecheck, tests, production builds),
Desktop 2,384 passed / 8 environment skips (MacTeX 5, Linux-only 1, live Claude/Hermes 1 each),
Briefing 664, Model Lab 341; Agent Runtime 635 (188 + 446 + 1). Local signing unit test passed.
Development directory package and isolated startup passed. Full certificate signing is currently
waiting in macOS SecurityServer private-key signature generation; user approval was requested.
Candidate may be partially signed while the process is pending and must not be installed yet.
No installed app replacement, user settings/data mutation or installed UI verification is claimed.
