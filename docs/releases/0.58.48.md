# GOSU 0.58.48 candidate

Critical Review activates two author-side modes using the existing Project Chat runtime.
See [implementation and research basis](../CRITICAL_REVIEW.md).
Follow the [release runbook](../RELEASE_RUNBOOK.md) and [maintenance guide](../MAINTENANCE_GUIDE.md).

Status: verified candidate, **not installed**. The existing 0.58.40 installation and user
settings/history are untouched. All accumulated working-tree changes remain; no commit/push is
part of this request. Main-tree base is `a4d5ef7effe0f863f799d646236f97b01898d94a` plus earlier and
current uncommitted changes; this is not a build of the separate plan-only published branch.

## Implemented and verified

- Active project Critical Review entry; research-direction and manuscript-review modes with
  independent saved histories and new-review controls. Shared attachments, model selection,
  chat context/usage, cancellation and queued-message behavior are reused.
- Typed immutable session mode, additive SQLCipher migration, branch inheritance, server-forced
  advice-only execution, allowlisted read catalogs and dispatch rejection of forced write calls.
- Manuscript reviews omit automatic private project objectives/tasks/permanent-memory context
  and do not expose project/Notes/experiment tools. Advice is not an independent human review.
- Focused four-file regression gate: **203 passed**.
- `pnpm check`: **3,940 passed**, **8 existing environment skips**. Desktop 2,540; Briefing 773;
  Model Lab 349; Contracts 45; Briefing Core 116; UI 15; Integrations 19; Domain 14; Sync API 51;
  root scripts 18. Full formatting, generated-contract checks, lint, typecheck and production
  builds passed. Initial sidebar-order expectation and missing release navigation were corrected
  before the final successful full gate.
- `pnpm test:agent-runtime`: **1,223 passed** (30 + 642 + 511 + 40); includes the new critic gate.
- Native SQLCipher smoke passed, including durable review mode/history restoration and
  project-scoped lookup. Existing plan atomicity and context regressions also passed.
- Real browser rendering of the actual header and ProjectChatView with synthetic inputs at
  1280×720: both modes, selected indicator, artifact-only footer, attachment/input controls,
  new-review history preservation and selecting an old review were checked. The fixture's initial
  missing message-actions array/model-provider was corrected; no production renderer defect was
  inferred from that fixture error. Temporary browser and preview server were closed.
- Skips: five MacTeX opt-in, one Linux-only, live Claude and live Hermes opt-in. No actual private
  manuscript or live LLM review was run; critic accuracy/calibration has not been benchmarked.

## Package and installation

- Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, Info.plist **0.58.48**.
- Asar SHA-256: `ce8a0daabb3c00dcca654b8d1504c1bef83c5b1368b5a3b00f8248fc04e85ce1`.
- Directory packaging, fixed-certificate strict app/helper signing and isolated startup passed.
  Signer: GOSU Local Code Signing, `093F07514506D9D61282F362792B33741C834264`.
- Update continuity: `preservedIdentity=true`, `requiresOneTimeApproval=false`; this is not a
  guarantee that macOS will never display a permission dialog.
- `/Applications/GOSU.app` remains **0.58.40**, with PID 39946 still present. No forced quit,
  SecurityAgent/Keychain ACL/TCC workaround, backup/swap or user-data reset was performed.
  Safe normal shutdown is required before replacement. Installed UI verification remains pending.
  Existing protected backups are described in [0.58.40](0.58.40.md).
- Older binaries do not understand the review-mode field and must not be relied on to enforce
  its read-only boundary after rollback. Preserve the updated database and follow the runbook.

Logs in `/private/tmp`: `gosu-critical-focused.log`, `gosu-critical-final-check.log`,
`gosu-critical-final-runtime.log`, `gosu-critical-db.log`, `gosu-critical-package.log`,
`gosu-critical-sign.log`, `gosu-critical-continuity.log`, `gosu-critical-startup.log`.
