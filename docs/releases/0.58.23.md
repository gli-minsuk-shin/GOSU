# GOSU 0.58.23 — candidate, deployment blocked on stable signing

Preserves any existing current/legacy Briefing settings on automatic bootstrap, including empty,
default-looking, corrupt or unreadable state. Explicit restore is an in-memory draft until saved.
See [workspace contract](../BRIEFING_WORKSPACE.md) and [release procedure](../RELEASE_RUNBOOK.md).

Installed GOSU uses a cdhash-only designated requirement; development packaging explicitly forces
ad-hoc signing. Read-only `security find-identity -v -p codesigning` returned zero valid identities.
The new pre-update continuity gate rejects ad-hoc candidates or unexpected identity changes.
No certificate/key, permission, account or saved setting was changed. Do not deploy another ad-hoc
candidate while claiming Calendar permissions will persist.

## Verification and installation boundary

- `pnpm check`: format, generated contracts, lint, typecheck, workspace tests, root script tests
  and production builds passed. Desktop 2,380 passed / 8 environment skips; Briefing 655 passed;
  Model Lab 341 passed; UI 15 passed. Desktop skips: MacTeX opt-in (5), Linux-only (1), live
  Claude (1), live Hermes (1).
- Bootstrap preservation regressions: 4 passed; signing-continuity regressions: 4 passed.
  Agent Runtime: 625 passed (184 Desktop, 440 Briefing, 1 Model Lab).
- Development directory package and isolated startup passed. Candidate version 0.58.23,
  `app.asar` SHA-256 `6ee5838b6bfa44e707256b8f287a0c1e3d2c89a09d1c19961c20584632f34985`.
- Real continuity gate rejected the candidate with the expected ad-hoc diagnostic. This is an
  intentional deployment block, not a passing release-continuity claim. Installed app remains
  0.58.20; no Quit, replacement or OS permission manipulation was performed.
- Stable signing setup was offered to the user. A one-time reviewed identity transition may
  still require OS consent; no credential was generated/imported, and no authorization was assumed.
- The older 0.58.14 default-looking workspace auto-restore policy is superseded by this release.
  Source is the dirty working tree at `a4d5ef7effe0f863f799d646236f97b01898d94a`; no commit/push.

Reference: [Apple designated requirements](https://developer.apple.com/documentation/technotes/tn3127-inside-code-signing-requirements).
