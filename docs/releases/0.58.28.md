# GOSU 0.58.28 — exclude saved papers before limiting

See the [release runbook](../RELEASE_RUNBOOK.md) and
[Briefing contract](../BRIEFING_WORKSPACE.md).

Installed 2026-09-13. arXiv collection excludes exact summarized identities/versions before
applying the configured display count within the same bounded response (no query widening).
Cached raw XML is re-filtered for each saved-summary set without another HTTP request. Scholar
merging and final duplicate filtering remain; existing summaries and settings are not rewritten.
Failed results without an error string are no longer described as a genuine zero.

Verification: focused tests 42 passed; full `pnpm check` passed format/contracts/lint/typecheck,
workspace tests and production builds. Briefing 679, Desktop 2,386 passed / 8 existing environment
skips (MacTeX 5, Linux-only 1, live Claude/Hermes 1 each), Model Lab 341. Agent Runtime 652
(190 + 461 + 1). Regression verifies fresh and cached requests reveal the previously hidden item,
and the production collection hands saved identities to the parser before limiting.

- Git HEAD: `a4d5ef7effe0f863f799d646236f97b01898d94a`, dirty local tree; no commit/push.
- Artifact: `apps/desktop/dist/mac-arm64/GOSU.app`; installed `/Applications/GOSU.app`.
- `app.asar` SHA-256: `6f7cfd1d49e348d6ad042670b8f2de9bf140a651088a73fc276e4545f2458378`.
- Backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.28-20260913-071635.app`.
- Normal native Quit and process/port closure verified. Staging version/hash/strict signature
  matched; installed strict signature and isolated startup passed. App/helper retain the same
  local certificate; continuity gate reported preserved identity, no signing migration needed.
- Installed Main header and screenshot show 0.58.28. Existing project/chat content is visible;
  Briefing server listens on 4318. Native UI automation could not reliably open the Briefing pane.
- Two bounded read-only arXiv verification attempts, separated in time, timed out. They were
  reported unverified, not zero. No new live paper summary or provider availability is claimed.
  The repaired selection is covered by regression tests; no live LLM call was made for validation.
- User settings, source scope, permissions and old summaries/history were not changed or reset.
