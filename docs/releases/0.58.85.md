# GOSU 0.58.85 installed; repaired revision saved

See [graph validation and compiler repair](../MODEL_GRAPH_PRESENTATION.md) and
[release procedure](../RELEASE_RUNBOOK.md). Includes pending 0.58.84 chat continuity changes.
Cumulative dirty source; no commit/push.

Fixes false equality-chain detection from nested axis/index qualifiers, aggregates name and
structural validation findings, and supports one bounded compiler repair with actual feedback.
Keeps full audit, stable identity, uncertainty and explicit apply boundaries. A same-owner matching
loop-carried arrow redundantly emitted as an ordinary edge is canonicalized to the binding pair;
invalid ownership/binding/shapes and external edges remain rejected. Saved source is no longer
described in the prompt as already validated. Native read-only inspection and compiler handoff are
explained separately. Compiler usage includes both attempts and is preserved on failure.

Authorized real-model execution: an initial high-reasoning generation timed out at five minutes.
A medium-reasoning generation and one repair returned candidates. The old validator rejected
names/loop representation and later miscounted nested formula qualifiers as assignments. The fixed
validator accepted the second candidate with seven modules, nine connections and seven presentation
records. ModelIR→pseudocode→parse roundtrip passed. Unresolved internal score/transposition/residual
questions remain explicitly unresolved; no runtime execution was performed. Private input/output
stays outside the repository in a protected temporary directory; no research content is copied here.

## Verification and installation

Focused 62 passed. Full check passed: 4,211 tests / eight existing environment skips (MacTeX five,
Linux-only one, live Claude/Hermes one each). Model Lab 419, Briefing 915, Desktop 2,599,
other packages 260, root scripts 18. Named Runtime gate 1,734 passed
(14 + 157 + 819 + 563 + 181). Typecheck/lint/format/contracts and production builds passed.
Real-render isolated preview of the generated model showed the overall graph and four repeated
internal stages with formulas and symbolic shapes, not unknown/huge dimension placeholders.
Preview inference endpoints were disabled and its temporary workspace was removed after inspection.

Default packaging hit an Electron download HTTP 504. Packaging was rerun with the existing local
Electron 43.2.0 distribution (version checked) through electron-builder's electronDist option.
Packaged and installed startup smoke passed. Fixed certificate signing and strict app/helper checks
passed; update continuity preservedIdentity=true and requiresOneTimeApproval=false.

Source: `apps/desktop/dist/mac-arm64/GOSU.app`; installed `/Applications/GOSU.app`, 0.58.85.
ASAR SHA-256: `c246d1a7dfed0991485e45df0d00ba3608290dd9b556e9ef22437571c19fa035`.
Certificate: `093F07514506D9D61282F362792B33741C834264`.
App was no longer running before backup; no forced quit. Data backup, 77 files / 8,794,814 bytes:
`/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.85-koSW2g`.
Backup freshness and inactivity were rechecked before staged replacement. Previous binary:
`/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.85-2026-09-14T15-17-15.444Z.app`.

## Authorized model repair application

The requested model's current r0 was compared against the generation base and was unchanged.
After installation, while GOSU remained closed, its project-scoped ModelLabDesktopHost storage API
saved the validated candidate as r1 using the existing immutable revision builder. The original
stored r0 object, other model histories and all other workspace keys (including chats/memory) were
verified unchanged. Only the requested model's revision was appended and selected. No experiment,
Python execution or automatic training was run. Ambiguous internal equations remain annotated.
Private generation/proposal artifacts were retained in the protected app backup area rather than Git.

Actual app relaunch is waiting in macOS Security SecItemCopyMatching/keychain content access;
native UI inspection timed out. Installed binary/startup smoke and saved r1 verification are complete,
but the actual installed-window r1 display must be checked after the user handles the OS prompt.
The isolated updated-client preview is not misreported as the installed window. Model routing,
approval policy, shortcut and language files remain byte-identical. No keychain ACL, credential or
OS permission was changed to bypass the wait.
