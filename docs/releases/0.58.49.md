# GOSU 0.58.49 installed

See [model references](../MODEL_REFERENCES.md) and the [release runbook](../RELEASE_RUNBOOK.md).

## Installed on 2026-09-14

The former process had exited before replacement; absence of a GOSU process and database WAL was
verified. The validated candidate was copied through `/Applications/.gosu-update-0.58.49-ZTFQbV`,
rechecked, then swapped without touching live user-data paths. The empty staging directory was removed.

- Installed target: `/Applications/GOSU.app`, **0.58.49**.
- Installed asar SHA-256: `383368411cd93201c71a23f1b27349916bf594306eaa4cf13d40f5e44441f664`.
- Previous binary: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.49-20260914-082929.app`.
- Protected state backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.49-20260914-082929`.
  SQLCipher DB (3,895,296 bytes) and encrypted Briefing workspace (2,306,250 bytes) were byte-compared
  with their sources before launch. Backup directory is 0700; files are 0600. No WAL existed.
- Staging and installed version/hash/strict signature checks passed. Update identity was preserved;
  installed isolated startup passed. No keychain ACL, TCC, certificate trust or account grants changed.
- Normal app launch initially timed out during window discovery. After startup, the installed
  main process owned the Briefing listener and native UI discovery succeeded. Header **v0.58.49**,
  existing project list/Project Chat history, saved model selection and Critical Review menu were
  observed. No private conversation content is copied into this record. Individual new workflows,
  global assistant history display and live LLM inference were not exercised during installation.
- The unified always-approve option remains unimplemented and is not in this release.

The earlier candidate notes below describe the pre-installation state and verification history.

Verified source; installation was pending safe shutdown of the existing 0.58.40 process. Main-tree
base is `a4d5ef7effe0f863f799d646236f97b01898d94a` plus accumulated uncommitted changes. No commit,
push, project-setting reset or user conversation/model modification was performed in this task.

## Behavior

- Model rows and the active model heading expose Project Chat and internal Model Lab chat buttons.
  The local path selects the model, reveals the copilot and focuses the composer. The Project Chat
  path flushes saves, validates iframe source/origin and re-resolves saved identity in Main.
- Model-bound sessions retain the model name, root revision and normalized-source hash, reuse an
  existing exact-reference conversation and preserve the tag in SQLCipher across restarts/branches.
  New-context focus uses stable session/hash identity rather than taking focus on every snapshot.
- Project Chat can read its own model catalog, structure, pseudocode and archived conversations.
  The global assistant can read these across approved active projects through existing project-read
  and private-AI gates. Reads do not create an archive, regenerate a model, train, edit or forward
  whole projects automatically. Missing/corrupt/trashed evidence is not reported as empty success.
- Catalog/text pagination and legacy-history limitations are explicit. Root references do not
  claim to freeze every transitive nested-model version. No LLM call is made just to open a chat.
- Existing Critical Review manuscript-only read restrictions are preserved; direction review may
  inspect model evidence. Project Chat policy provenance advances to version 40.

## Verification

- Final `pnpm check`: **3,954 passed / 8 existing environment skips**. Desktop 2,547; Model Lab 356;
  Briefing 773; Contracts 45; Briefing Core 116; UI 15; Integrations 19; Domain 14; Sync API 51;
  root scripts 18. Formatting, generated contracts, lint, typecheck and production builds passed.
- Named Agent Runtime gate: **1,236 passed** (30 + 649 + 511 + 46), including model-reference
  reader/handoff, project provider/context tools, global private-AI gating and Model Lab storage.
- Native SQLCipher smoke passed, including new model-reference field readback after restart,
  existing Critical Review mode/history restoration and research-plan atomicity checks.
- Focused gates: model reader/host/environment/context 25 passed; assistant bridge 19 passed;
  Desktop reader/service/prompt/handoff/global tests 198 passed before the additional chip test;
  final chip/typography/error-translation recheck 14 passed. Full final gates include all additions.
- Real 1280×720 browser fixture used the actual ModelLabApp and ProjectChatView. Both action
  buttons, local composer focus, parent handoff, Project Chat chip/focus and the model tag's own
  visible row were checked. Fixture storage/provider responses were isolated, with all provider
  calls disabled. Initial fixture-relative-import and API-stub issues are not live-app failures.
  Browser/server were closed; no private models or actual LLM inference were used.
- Initial gates caught import/style issues, old prompt-version expectations, missing test-window
  listener support and a new untranslated error. These were corrected before the final passes.
- Skips: five MacTeX opt-in, one Linux-only, live Claude and live Hermes opt-in. No scientific
  quality/recall benchmark or installed-user-workspace UI test is claimed.

## Installation boundary

2026-09-14 installation follow-up: the user reaffirmed automatic installation after verified
changes, without a separate request. Rechecked the installation: 0.58.40/PID 39946 still running.
Exact `/Applications/GOSU.app` native window inspection timed out again; idle state and safe Quit
could not be verified. No forced termination, swap or new backup occurred. The unified always-approve
option discussed afterward has not been implemented and is not included in this artifact.

The candidate is `apps/desktop/dist/mac-arm64/GOSU.app` version **0.58.49**.
Asar SHA-256: `383368411cd93201c71a23f1b27349916bf594306eaa4cf13d40f5e44441f664`.
Final directory packaging, fixed-certificate strict app/helper verification, continuity and isolated
startup passed. Signer: GOSU Local Code Signing, `093F07514506D9D61282F362792B33741C834264`.
Continuity reports `preservedIdentity=true`, `requiresOneTimeApproval=false`; this is not a guarantee
that macOS will never display a permission prompt. Installed `/Applications/GOSU.app`
is still 0.58.40 with PID 39946 present; no forced quit or SecurityAgent/Keychain ACL/TCC workaround
was used. No new backup/swap occurred. Existing protected backups are in [0.58.40](0.58.40.md).
Do not use an older binary that ignores the new reference/mode fields to enforce these boundaries
after rollback; follow the runbook and preserve the updated database.

Logs under `/private/tmp`: `gosu-model-reference-final-gate.log`,
`gosu-model-reference-verified-runtime.log`, `gosu-model-reference-db.log`,
`gosu-model-reference-final-package.log`, `gosu-model-reference-ui-recheck.log`,
`gosu-model-reference-final-sign.log`, `gosu-model-reference-continuity.log`,
`gosu-model-reference-startup.log`, `gosu-model-reference-final-format.log`.
