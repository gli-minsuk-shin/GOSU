# GOSU 0.58.77 candidate

Compact idle Briefing header; see [Briefing workspace](../BRIEFING_WORKSPACE.md) and
[release runbook](../RELEASE_RUNBOOK.md). Cumulative dirty source at
`303360b9b729d65bf568f79da3fbe1a137ccb67c`, no commit/push.

Removed the completed-job detail, next-run timestamp and duplicated job/source error from the top
generation controls. The idle header retains title, collapse, schedule selector and generation icon.
Running progress and cancellation remain, as do failed explicit actions and schedule/load errors.
Source result warnings in Briefing content and backend job/schedule records are not changed or erased.
Automatic interval restoration, polling and history-update events remain unchanged.

Regression first failed on the old status row, then passed after removal. Focused tests: 12 passed.
`pnpm check`: 4,120 passed, 8 existing environment skips (MacTeX 5, Linux 1, live Claude/Hermes 1
each); format/contracts/lint/typecheck/production builds passed. Runtime: 1,498 passed
(108 + 795 + 541 + 54).
Production-controls visual fixture received a completed partial-source failure and next-run time:
wide idle header measured 48px, no status/duplicated error rows. Narrow 380px layout retained all
buttons without clipping. Simulated generation retained collapsed progress and the stop button.
No real generation/source/account call occurred. Closed both preview tabs and the local server.
Installed app signing continuity remains subject to the previous ad-hoc installation issue;
do not bypass the gate or reset existing settings/history.

Prepared `apps/desktop/dist/mac-arm64/GOSU.app` 0.58.77 passed packaged isolated startup and fixed
certificate app/helper strict verification (`093F07514506D9D61282F362792B33741C834264`).
ASAR SHA-256: `d79386874013c6f9f2b187bbf7f5dd8dba48eb4c0a1ddff0927eb62a3276fa4c`.
Continuity against `/Applications/GOSU.app` failed because its signing identity differs. No installed
app replacement, shutdown, data backup or OS permission change occurred in this task. Restoring the
fixed signing identity requires explicit user review before the normal backup/replacement flow.
