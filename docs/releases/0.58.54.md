# GOSU 0.58.54 candidate

Subsequently included and installed with [0.58.55](0.58.55.md); this candidate was not installed separately.

See [approved scope reuse](../APPROVED_SCOPE_REUSE.md) and [release runbook](../RELEASE_RUNBOOK.md).
Includes the pending 0.58.53 paper discovery recovery and app-wide reuse of existing approved scopes.
No new account/root/OS permissions are silently granted. Existing configured scope, exact binding,
revocation and audit controls remain. User asked to apply reuse across GOSU's already approved work.

## Verification

- Focused policy/SSH/log service tests: 91 passed; Briefing scope/store/host/server: 25 passed.
- `pnpm check`: 3,981 passed (3,963 workspace + 18 root), 8 existing environment skips
  (MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each).
  Formatting, generated contracts, lint, typecheck and production builds passed.
- `pnpm test:agent-runtime`: 1,367 passed (62 + 740 + 519 + 46).
- Real-render synthetic settings fixture: checked default, saved unchecked state, compact 16×16px
  checkbox aligned with its label, no horizontal overflow. Fixture does not modify real permissions.
- Regression coverage: current scope only, foreign project/command rejection, root/diagnostics/
  changed connection ineligibility, audit-before-execution, policy-off race, persisted revocation,
  restart/default/invalid-file handling, concurrent off+revocation, failed UI save preserving value,
  Briefing owner/digest changes and host-to-policy wiring.
- No real remote command, private Mail/Calendar mutation or LLM work was used for testing.
- Git HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`; dirty local changes include prior updates.
  No commit/push performed in this update.

Installation pending. Installed app remains 0.58.52; 0.58.53 was not installed. The installed native
window lookup is still timing out; no force quit or replacement while running is permitted.

Candidate `apps/desktop/dist/mac-arm64/GOSU.app` version 0.58.54 was directory-packaged, fixed-signed
and verified strictly (main/helper). Isolated packaged startup passed. Continuity against the
installed app reports preservedIdentity=true/requiresOneTimeApproval=false; this is not an OS prompt
guarantee. Candidate `app.asar` SHA-256:
`ac9d39b8c4c96746acd43c5aa7a5cf4e46f436ef9d1b357f8dbc32a2ac955d99`.
Final safety-copy clarification was rechecked with the two policy/UI tests and a fresh production
package; 16px checkbox layout was visually verified. No public notarization or new certificate.

Read-only sampling of the current installed process showed its main thread waiting in
SecItemCopyMatching/SecKeychainItemCopyContent, with SecurityAgent present. The user must resolve
the macOS keychain prompt and quit normally before installation. No security dialog automation,
Keychain ACL changes, key extraction, forced termination or data reset was attempted. No 54 app/data
backup or installed-54 UI verification exists yet; perform fresh protected backups after confirmed
exit according to the runbook. Existing 52 app and user data are unchanged.
