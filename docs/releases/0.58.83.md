# GOSU 0.58.83 installed; interactive verification pending

Explicit email/private summary refresh permission recovery. Includes pending 0.58.82 vote toggling.
See [Briefing intelligence](../BRIEFING_INTELLIGENCE.md) and [release procedure](../RELEASE_RUNBOOK.md).
Cumulative dirty source at HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`; no commit/push.

Read-only inspection found currently enabled mail read/private AI and a valid saved approval.
The earlier protected backup, with settings timestamp close to the reported screenshot, had mail
read disabled while private AI and approval were enabled. The refresh branch incorrectly reported
that as missing private-provider approval. Current enabled settings were not rewritten. Synthetic
regression reproduced the dead-end when saved permissions are enabled but approval is invalid.
Previously refresh returned a generic settings error; it now requests native consent for the exact
saved profile, then continues. Valid approval does not prompt. Disabled read/AI permissions have
distinct messages and remain disabled. Foreign ownership is rejected. Decline and concurrent edits
preserve settings/history and prevent source/model calls. Live receipts and stored history share the gate.

Four focused expectations and one settings warning expectation failed before implementation.
Settings now show explicitly when private AI is enabled but mail read is off.
Final verification and installation results are recorded below.
No private email was sent to a model during diagnosis; settings were inspected, not changed.

Real-render synthetic settings QA verified the warning beside the separate switches and its
removal when mail read is enabled. No real settings/OS permission was changed in that fixture.
The app was no longer running before backup; no forced quit was used. Protected byte-verified
backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.83-hli7xx`
(76 files, 8,777,788 bytes). Recheck inactivity and data freshness before any swap.

## Final gates and installed state

Focused 32 passed. Full check 4,195 passed / eight existing environment skips (MacTeX five,
Linux-only one, live Claude/Hermes one each). Briefing 914, Desktop 2,596, Model Lab 407,
other packages 260, root scripts 18. Runtime 1,636 passed (140 + 818 + 562 + 116).
Typecheck/lint/format/generated contracts and production builds passed. Synthetic real-render
settings QA passed. Package and installed isolated startup smoke passed.

Source artifact: `apps/desktop/dist/mac-arm64/GOSU.app`; installed `/Applications/GOSU.app`, 0.58.83.
ASAR SHA-256: `1a42804a02d528af87d81b5edbaa5082cc5ba5cac7deff5df0dec9d0fd27d59a`.
Fixed certificate `093F07514506D9D61282F362792B33741C834264`; strict app/helper verification
and continuity passed, preservedIdentity true / requiresOneTimeApproval false.
Inactivity and backup bytes were rechecked before staged swap. Old binary retained at
`/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.83-2026-09-14T14-06-21.439Z.app`.
Model routing, approval policy, shortcut and language files remained byte-identical after launch.

Actual app launch is waiting in macOS Security `SecItemCopyMatching`/keychain content access.
Native UI inspection timed out. Do not confuse isolated startup success with actual workspace UI
or email re-summary success. The user must handle any macOS keychain dialog; no ACL, credentials,
OS grant or stored preferences were changed to bypass it. Interactive version/settings and the
reported email refresh remain to verify after that wait ends. The app was not force-quit.
