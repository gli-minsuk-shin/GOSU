# GOSU 0.58.60 candidate

See [Briefing workspace](../BRIEFING_WORKSPACE.md) and [release runbook](../RELEASE_RUNBOOK.md).
Includes the pending [0.58.59 Scholar discovery fixes](0.58.59.md).

Removes the same-day Calendar skip from briefing generation. Every generation reads the approved
today/tomorrow range and replaces today's agenda after a successful guarded read. Added/edited/
deleted events are reflected, empty success clears the agenda, and failed reads preserve previous
events with a warning. Weather and existing email/paper summaries are reused as before.

Focused generation tests: 26 passed, including a same-day add/edit/delete/failure regression.
Read-only live verification used the installed signed Calendar bridge with the current approved
calendar IDs and today/tomorrow window: 3 current events versus 2 in the saved agenda, with 1
current event absent from that agenda. No event was created, edited or deleted for verification.

`pnpm check`: 4,004 passed (3,986 workspace + 18 root), 8 existing environment skips
(MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each). Briefing 811, Desktop 2,557,
Model Lab 358. Format, generated contracts, lint, typecheck and production builds passed.
`pnpm test:agent-runtime`: 1,422 passed (74 + 772 + 528 + 48).

Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c` with existing dirty work preserved.
No commit/push. Installed app remains 0.58.58; its active generation has not been interrupted.

## Prepared installation

Candidate `apps/desktop/dist/mac-arm64/GOSU.app` version 0.58.60 passed packaged isolated startup,
fixed-certificate app/helper strict signature verification and update continuity
(preservedIdentity=true/requiresOneTimeApproval=false). Its app.asar SHA-256 is
`efb2ceb0d2cf8372427ccc37c0046c1df48bce64e40363137d3bc07e7399d05a`.
The final native UI check still showed disabled Generate and active Stop generation. No forced
termination, backup swap, data overwrite or installed end-to-end generation was performed.
Await user normal termination; then take fresh backups and follow the runbook. This candidate
supersedes the 0.58.59 binary and includes those source changes.
