# GOSU 0.58.33 — durable assistant and verified paper library

Installed, 2026-09-13. Follow [release procedure](../RELEASE_RUNBOOK.md) and
[paper library contract](../SHARED_PAPER_LIBRARY.md).

Includes pending encrypted assistant conversations, verified arXiv paper ingestion using the summary
model role, selected library deletion, and the composer Settings slider icon. Existing settings and
private-data grants are unchanged. DOI/publisher ingestion is not supported. No real user paper is
deleted or summarized by installation. Conversation persistence starts with this version; old
page-only messages are not automatically migrated.

New defaulted workspace fields are incompatible with older strict readers after a write. Preserve
the new encrypted data and do not perform a blind binary rollback. No destructive data migration.

Source is the dirty working tree at a4d5ef7effe0f863f799d646236f97b01898d94a; no commit/push.
Prior synthetic UI verification covered restored conversations, paper selection/confirmation and
the matching Settings icon.

## Verification and installation

- `pnpm check` passed: Briefing 713; Desktop 2,394 passed / 8 existing environment skips
  (MacTeX opt-in 5, Linux-only 1, live Claude/Hermes 1 each); Model Lab 341.
  Formatting, contracts, lint, typecheck and production builds passed.
- Agent Runtime 679 passed (197 Desktop + 481 Briefing + 1 Model Lab).
- Candidate `apps/desktop/dist/mac-arm64/GOSU.app` passed directory packaging, isolated startup,
  fixed local-certificate signing and strict app/helper verification. Update continuity returned
  preserved identity and no required one-time approval. This is not public notarization.
- Candidate/staging/installed asar SHA-256:
  `f045f72cf71f26891780315680e5ddbb5b2c996c4b6f3e4b0af967c7f450befe`.
- Previous app backup:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.33-20260913-release.app`.
- Pre-update encrypted workspace backup (byte-identical and original private mode preserved):
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/workspace-before-0.58.33-20260913.enc.json`.
- Native UI changed during inspection, so the user was asked to quit normally. Process absence was
  verified before replacement; the user confirmed quitting. No force kill. The final observed assistant
  input/history were empty, so no stale earlier draft was reinserted.
- Verified staging `/Applications/.gosu-stage-0.58.33.EpP5tD` was moved into place and its empty
  directory removed. Installed `/Applications/GOSU.app` strict signature/hash/startup passed.
- Actual accessibility and screenshot showed v0.58.33, retained project navigation/chat, and the
  matching slider Settings icon in the assistant composer. No test question, inference, paper save,
  paper deletion, settings write or permission change was initiated during installed UI inspection.
- Live end-to-end paper ingestion and cross-restart user conversation persistence were not exercised;
  their source regressions and synthetic UI verification are distinct from installed icon verification.
