# GOSU 0.58.71 installed — startup Keychain wait

See [model references](../MODEL_REFERENCES.md) and [release runbook](../RELEASE_RUNBOOK.md).
The main model AI-conversation action now only opens Model Copilot and its selected model/revision
chip. Project Chat handoff remains explicit in the existing model menu. The hosted model row grid
allocates separate space to the AI button and menu. Existing conversation and Project Chat's
same-project Model Lab reader permissions are unchanged. No automatic question or model write.

## Verification

- Focused routing/access regressions: 76 passed (handoff/UI, project tools and global assistant).
- `pnpm check`: 4,073 passed (4,055 workspace + 18 root), 8 existing environment skips
  (MacTeX opt-in 5, Linux-only 1, live Claude/Hermes 1 each). Desktop 2,590, Briefing 841,
  Model Lab 364. Format/contracts/lint/typecheck/production builds passed.
- `pnpm test:agent-runtime`: 1,465 passed (80 + 793 + 538 + 54). New local-action routing
  regression is included in the existing handoff suite in the named gate.
- Hosted synthetic fixture uses the actual ModelLabApp with two models and mocked API responses.
  Clicking model B after collapsing Copilot reopened it, showed `Imported QA B · r0`, focused the
  Model Copilot textarea, and recorded zero Project Chat handoffs and zero LLM calls. Initial
  malformed fixture copy-list response was corrected; no user model or conversation was changed.
- Project Chat's same-project model reader and explicit scoped handoff were retained and tested.
- Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`, existing dirty work preserved, no commit/push.
  Earlier uninstalled 0.58.67–70 changes are included. Installed UI verification remains pending below.

## Installation

Installed `/Applications/GOSU.app` 0.58.71 from `apps/desktop/dist/mac-arm64/GOSU.app`.
Source, staging and installed ASAR SHA-256:
`d3eb7ac8264d31e86e718c4a7d67b8b27e9caab99567d741218971e1f33ab908`.

- Native UI access recovered. The old app showed an idle Calendar view with no edit dialog or
  visible active-job controls. Normal GOSU menu Quit succeeded. Process exit/no DB WAL were checked.
- Previous 0.58.66 app: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.71-20260914-171424.app`.
- Protected state: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.71-20260914-171424`.
  71 files, 8,554,039 bytes verified byte-identical. Includes DB, model routing, approvals, shortcut,
  language, usage ledger, Preferences/Local State, complete Model Lab and Briefing directories, Local
  Storage and Session Storage. Backup root is 0700. Original state was not restored or replaced.
- Fixed-certificate app/helper strict signing and continuity passed (`preservedIdentity=true`,
  `requiresOneTimeApproval=false`); this is not a guarantee about every OS Keychain interaction.
  Verified staging was moved into place, empty staging removed, installed isolated startup passed.
- After launch, model-routing, assistant-shortcut and automatic-generation files remained
  byte-identical to backup. No settings, account grants, conversations or model data were reset.

## Remaining live startup verification

Cold native window lookup timed out twice. Main process started, but the Briefing port was not yet
listening. A read-only process sample at 17:20 KST showed the main thread blocked in
`SecItemCopyMatching`/Keychain content decryption. The sampled process reports installed version
0.58.71. A Keychain approval dialog was not directly observed; the user was asked to check for one
and approve only in the macOS dialog, never send a password through chat.

Do not bypass SecurityAgent, weaken Keychain ACL/trust, reset data or claim the installed renderer
has been verified yet. Await the user's OS-state response, then confirm the actual app header,
restored data and Model Copilot action. The full hosted fixture already verified the UI routing.
