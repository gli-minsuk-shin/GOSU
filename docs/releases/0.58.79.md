# GOSU 0.58.79 installed

Cumulative dirty source at HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`; no commit/push.
Includes 0.58.74–78 candidates plus compact graph toolbar, email body recovery, timed prepared
email tasks and Model Copilot graph-edit failure isolation. See [email actions](../EMAIL_PREPARED_ACTIONS.md),
[Model Copilot](../MODEL_LAB_CONTEXT.md), and [graph presentation](../MODEL_GRAPH_PRESENTATION.md).

Behavior gates passed before version bump: full 4,150 tests, eight existing environment skips
(MacTeX five, Linux one, live Claude/Hermes one each); Runtime 1,545 passed. Type/lint/format/build
and synthetic rendered UI passed. Synthetic live Astra explanation and five-module graph edit passed;
one Luna summary preserved separate event times and task deadline. No personal items were created.

User approved restoring the original fixed signing identity from the ad-hoc installation and
requested installation. Do not bypass OS dialogs. Settings/history remain outside the app bundle.
New optional local task dueAt is not compatible with older strict readers; do not downgrade user data
without compatibility review. Current installation is stopped; fresh backup, package/signature/hash,
replacement and installed UI verification are pending.

Pre-install protected backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.79-lx79gO`.
73 files / 8,555,089 bytes compared byte-identically while GOSU was stopped; no database WAL remained.
Installed predecessor is 0.58.75, ad-hoc requirement `ef9b831122eee0dbdc7a27211cbb271e99d0fe99`.
Reviewed migration restores certificate fingerprint `093F07514506D9D61282F362792B33741C834264`;
this may require one-time macOS approval. No OS trust/ACL/TCC modification is authorized or performed.

## Completed installation

2026-09-14: final version-bumped check passed (4,150 tests; eight environment skips) and Runtime
1,545 passed. Directory packaging and isolated startup passed. Original certificate signing and
strict app/helper verification passed. Reviewed identity-migration gate reported
`preservedIdentity: false`, `requiresOneTimeApproval: true` as expected for the ad-hoc predecessor.

Installed `/Applications/GOSU.app` version 0.58.79 after staged version/hash/signature verification.
Previous app: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.79-2026-09-14T12-02-30.068Z.app`.
ASAR SHA-256: `3c4620147f9e97620a87aae97cb238a3b1e94bab3a1e5b8dd5fabcd4b673ee6e`.
Installed strict signature and isolated startup passed; empty staging directory removed.

Actual installed UI: initial lookup timed out, subsequent lookup and screenshots confirmed header
v0.58.79, existing Project Chat history, connected Codex and independent Briefing/Paper summaries
sidebar rows. A navigation click did not visibly change the current page during this check, so detailed
installed Briefing navigation/source-link and live private Model Copilot behavior are not claimed.
Model routing, approval policy, shortcut and application language files compared unchanged after launch.
No user mail/task/calendar write or LLM request was made during installed UI verification. Earlier
candidate/pending statements above describe the pre-install phase and are superseded by this section.
