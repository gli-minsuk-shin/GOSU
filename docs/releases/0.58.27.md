# GOSU 0.58.27 — visible Briefing permission confirmation

Follow the [release runbook](../RELEASE_RUNBOOK.md) and
[Briefing contract](../BRIEFING_WORKSPACE.md).

Installed on 2026-09-12. Embedded private-data confirmations now use a foregrounded,
parented GOSU native dialog instead of a detached 90-second script dialog. Cancel remains the
default; abort and a missing host window fail closed. Standalone Briefing retains its existing
confirmation. No permission bypass, root trust change or Keychain ACL modification.

Release gates passed: Desktop 2,386 tests / 8 environment skips (MacTeX 5, Linux-only 1,
live Claude/Hermes 1 each); Briefing 673; Model Lab 341. Agent Runtime 646 (190 + 455 + 1).
Focused approval tests 4 and server/host tests 10 passed. Formatting, contracts, lint, typecheck,
production build and isolated packaged startup passed. Native Main sheet was observed in installed
GOSU, and the requested Mail-reading preference was verified persisted after approval.

- Git HEAD: `a4d5ef7effe0f863f799d646236f97b01898d94a`; dirty local tree, no commit/push.
- Artifact: `apps/desktop/dist/mac-arm64/GOSU.app`; installed: `/Applications/GOSU.app`.
- `app.asar` SHA-256: `74f689e4cd8a96c9eed568bbe95f024c22a6c5477f6008dcaa12d51ad1d40cc4`.
- Backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.27-20260912-204954.app`.
- Normal native Quit, process/port closure checked, staging hash/version/signature checked before
  replacement. No user-data/Keychain/TCC reset. App and Calendar helper use the fixed local
  certificate. Reviewed first-migration gate passed; installed/candidate same-identity gate now
  reports preserved identity and no additional signing migration needed.
- Installed header 0.58.27 and new parented approval sheet verified through UI. Existing Mail
  accounts, 3-day/100-message scope, body-preview and Codex model settings were retained.
- Live generation completed with no job error: 29 newly saved email summaries and zero new
  papers. Encrypted-store readback confirmed all 29 email items have nonempty summaries and
  Mail reading remains enabled. Installed UI showed the completion count and `New 29` header.
  This verifies Mail collection/AI/history delivery for this run, not every mailbox or the
  scientific correctness of all future model outputs. No original mail read flags/writes were
  requested. Existing weather and prior history remain intact.
