# GOSU 0.58.81 installed

[Prepared email actions](../EMAIL_PREPARED_ACTIONS.md) and [release procedure](../RELEASE_RUNBOOK.md).
Cumulative dirty source at HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`; no commit/push.

Owned saved email summaries missing task actions receive a conservative persisted deadline recovery
before history display. Explicit numeric/Korean dates and clocks are read from saved title/action/summary,
not original mail; no LLM/source call. Receipt year anchors omitted years; semester years are not dates.
Conflicting dates/times, missing year anchors, invalid/contradictory dates and unsupported zones stay
unresolved. Older date-only actions can recover missing clock metadata without replacing notes/title.
Explicit null AI assessments are not overridden. Original summary timestamps/content remain unchanged.

Briefing settings add an explicit one-time Reminders connection and persisted default list/export choice.
Tasks reuse it; a missing chosen list is not silently replaced. OS permission is never fabricated;
native status and scope are rechecked, and revoked permission still requires user action. GOSU-only
tasks remain possible. Preferences share the existing encrypted task-links store and survive restart.

Focused tests and final full/runtime/UI/package/installed verification are in progress.
Installed predecessor is 0.58.80. Do not close an unsaved task editor without user coordination.

## Final gates and installation

Full check: 4,182 passed, eight existing environment skips (MacTeX five, Linux one,
live Claude/Hermes one each). Runtime: 1,617 passed (133 + 818 + 550 + 116).
Typecheck/lint/format/contracts/production builds passed. Package and isolated startup passed.
Fixed certificate signing and continuity passed with preservedIdentity true and no new approval requirement.

After verifying no open task editor or running generation, native menu Quit stopped GOSU normally.
Protected data backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.81-AhgMJE`,
76 files / 8,734,194 bytes compared identically. Previous app:
`/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.81-2026-09-14T13-06-31.590Z.app`.
Installed `/Applications/GOSU.app`; ASAR SHA-256:
`8b3e5c857870056e1f1690d1b25c8c7edf748ee30cd7f6730096e888f0fa28e2`.
Staged/installed version, hash, strict signature and isolated startup passed; empty staging removed.
Actual relaunch showed v0.58.81. Routing/approval/shortcut/language files compared unchanged.

Installed UI verification used the reported legacy deadline item: Add task displayed 2026-09-21,
12:00 PM, Asia/Seoul with the recovery notice, without a summary-refresh/model call. The modal was
closed without submitting a task. The real GOSU Briefing settings exposed the new Reminders connection,
default list and save controls. It showed not connected during inspection; no OS approval or preferred
list was chosen on the user's behalf. Phone delivery remains unverified. User first-time OS approval
and preferred-list selection are still needed, not per-item permission requests.

UI automation note: a stale AX index expanded an unrelated saved-summary disclosure, which was
immediately collapsed again. Refresh a full AX tree after screenshots and derive current indices;
do not reuse old menu indices. Later checks used fresh scoped state. A final scroll was interrupted
by user activity; no further UI actions were attempted. Earlier pending notes describe preparation.
