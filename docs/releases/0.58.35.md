# GOSU 0.58.35 — installed

2026-09-13. Source includes [AI assistant attachments and queue controls](../CHAT_ATTACHMENTS_AND_QUEUE.md).
Project Chat's existing edit/delete/stop-and-next queue is retained; native Codex steer is separate.
User settings, files, conversations and account grants are not reset.

Follow the [release runbook](../RELEASE_RUNBOOK.md) and [maintenance guide](../MAINTENANCE_GUIDE.md).

## Verified gates

- `pnpm check` passed: Desktop 2,399 passed / 8 existing environment skips; Briefing 731;
  Model Lab 341; Contracts 44; Briefing Core 116; UI 15; Integrations 19; Domain 14; Sync API 51.
- Typecheck, lint, formatting, generated contracts, root script tests and production builds passed.
- `pnpm test:agent-runtime`: 895 passed (7 queue/attachment + 395 Desktop + 492 Briefing + 1 Model Lab).
- Local SQLCipher smoke passed, including atomic take, receipt persistence and branch isolation.
- Synthetic production-component browser inspection passed: file chip and queue edit/save controls.
- Package, explicit fixed-certificate signing, strict app/helper validation and isolated startup passed.
- Continuity: `preservedIdentity: true`, `requiresOneTimeApproval: false`.

The initial gate found legacy test expectations missing the new host argument and queue-list read;
these were updated. The new database fixture initially lacked valid running provenance/terminal
branch history and was corrected. Final gates passed; no production regression was hidden by skips.
Existing skips remain opt-in MacTeX/live-provider or platform-specific tests; no paid live test enabled.

## Artifact and installation

- Source version 0.58.35; Git HEAD `a4d5ef7effe0f863f799d646236f97b01898d94a`, dirty working tree.
  No commit/push; unrelated existing changes preserved.
- Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`; installed: `/Applications/GOSU.app`.
- Installed/candidate asar SHA-256: `97f9e066832cc2016cc6dfa13566fdb1639f5ff1139df3490432dddd541fbbd0`.
- Signing identity: `GOSU Local Code Signing`, fingerprint `093F07514506D9D61282F362792B33741C834264`.
  This is a local fixed certificate, not public notarization.
- Prior 0.58.34 backup:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.35-20260913-release.app`.
- Private-state backup directory:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.35-20260913`.
  Encrypted workspace 2,289,594 bytes and SQLCipher DB 3,850,240 bytes, byte-identical at backup;
  directory 0700/files 0600; no WAL/SHM present. No keys, credentials or conversation content copied into docs.
- GOSU was already closed. Confirmed no main process before swapping through verified staging;
  removed only the resulting empty staging directory. User data paths and permissions were not reset.
- Installed isolated startup passed. Native UI showed **v0.58.35**, retained conversation, existing
  model selection and the attachment control. Initial control was still in startup-disabled state;
  enabled picker/live model submission were not exercised in the installed app. Subsequent UI read
  showed the user had navigated to Model Lab, so navigation/work was not interrupted for testing.
  Synthetic picker/queue interaction and mocked transport tests are distinct from that live limit.
- Older strict workspace readers may not understand the new queue field: preserve this backup and
  do not blindly roll back the binary over newer state.

Logs: `/private/tmp/gosu-05835-final-check.log`, `gosu-05835-runtime.log`,
`gosu-05835-db-smoke.log`, `gosu-05835-package.log`, `gosu-05835-sign.log`,
`gosu-05835-continuity.log` (all under `/private/tmp`). Markdown mirrors checked byte-identical.
