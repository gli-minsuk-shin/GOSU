# GOSU 0.58.72 installed

Architecture-first [Model Lab graph presentation](../MODEL_GRAPH_PRESENTATION.md).
Installation follows the [release runbook](../RELEASE_RUNBOOK.md).
Source HEAD: `303360b9b729d65bf568f79da3fbe1a137ccb67c`, cumulative dirty worktree; no commit/push.

Focused Model Lab tests: 155 passed. Final `pnpm check`: 4,093 passed, 8 existing environment skips
(MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each); format/contracts/lint/typecheck/build
passed. `pnpm test:agent-runtime`: 1,465 passed (80 + 793 + 538 + 54).
Actual saved-model preview inspected: English labels, compact equal-height repeated stages, formulas
separated into details. Original saved model was not changed or committed as a fixture.

## Installation

Previous 0.58.71 startup wait resolved: native UI showed its version and retained history, idle with
an empty composer. Normal GOSU menu Quit succeeded; main process exited and DB WAL was absent.
No force quit or OS/security setting change was used.

- Source: `apps/desktop/dist/mac-arm64/GOSU.app`; target `/Applications/GOSU.app`, version 0.58.72.
- ASAR SHA-256: `c48d7b2079706dadc12c27607c7f0347dd8dd40d9e877940e1f42fade2038dc4`.
- App backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.72-20260914-1805.app`.
- State backup: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.72-Nvqtqo`.
  71 files / 8,554,126 bytes copied and compared byte-identically under a protected backup root:
  DB, routing/approval/shortcut/language/usage settings, Preferences/Local State, full Model Lab,
  Local Storage/Session Storage, full Briefing Lab store. Originals were not replaced/reset.
- Fixed signer `093F07514506D9D61282F362792B33741C834264`; strict app/helper verification passed.
  Continuity `preservedIdentity=true`, `requiresOneTimeApproval=false`.
- Staged and installed hashes match; isolated packaged startup smoke passed. Empty staging removed.
- Initial native UI lookup after launch timed out, but the next lookup succeeded without a security
  bypass. Actual header showed 0.58.72 and prior Project Chat history. Opened the saved project's
  Model Lab, verified English model name, six overview tensor connections, forward default,
  compact equal-height cards and four repeated internal stages in graph focus mode. Viewed actual
  installed screenshots. Model routing, approval, shortcut and language files remained byte-identical
  to their pre-update backups. No LLM run or model revision was created by this verification.

The private read-only preview was closed and its loopback process stopped after visual inspection.
Its full-equation detail preview is not claimed as mathematical
validation of the user's source. Unsafe old dimensions still require original-source correction.
