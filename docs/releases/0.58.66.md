# GOSU 0.58.66 candidate

Follow the [release runbook](../RELEASE_RUNBOOK.md) for installation and rollback.

Adds [AI email task drafting](../BRIEFING_TASKS_AND_LIGHTWEIGHT_MODELS.md) using the configured
lightweight model, actionable title/notes and supported deadline. Missing deadlines remain empty;
date-only task storage keeps explicit clock times in notes, not timed reminders. Existing reviewed
GOSU/Reminders writes, idempotency, model settings and user history remain unchanged.

## Verification

- Focused: 40 passed across task AI helper, task modal, Calendar integration and source-service tests.
  Covers deadline/no-deadline, invalid dates/quotes, cancellation/revocation, private ownership,
  no native writes during drafting, double clicks and preserving edits across reopen/authorization.
- Agent Runtime: 1,446 passed (79 + 782 + 537 + 48).
- Synthetic real Spark/low inference: 6.764 seconds, correctly selected the final September 20
  deadline instead of the cancelled September 18 deadline or October 3 event. No personal source
  was transmitted or personal task/reminder created. A single example is not a speed/quality SLA.
- Actual browser rendering of a mocked email showed populated task title, notes and date in the
  existing compact review dialog; project and iCloud list selectors remain. No save was submitted.
- `pnpm check`: 4,048 passed (4,030 workspace + 18 root), 8 existing environment skips
  (MacTeX 5, Linux-only 1, live Claude/Hermes 1 each). Desktop 2,573, Briefing 839, Model Lab 358.
  Format, contracts, lint, typecheck and production builds passed.
- Initial test formatting/type-only import and missing doc-link
  issues were corrected. The synthetic CLI loader failed before inference; Vite's module loader
  subsequently ran the actual provider job successfully.
- Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`; existing dirty work preserved, no commit/push.

## Candidate ready; installation awaits normal quit

Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, version 0.58.66.
ASAR SHA-256: `c0bf18f02339f1e58e2dac710352feb6b210b765162e7a80555c13e7abf5a893`.
Packaging, isolated startup, fixed-certificate app/helper strict signing and update continuity
(`preservedIdentity=true`, `requiresOneTimeApproval=false`) passed.

Installed `/Applications/GOSU.app` remains 0.58.65. Its current UI showed no active generation or
edit dialog, but the normal menu action was rejected because the app changed; the required fresh
state read then timed out. No forced quit, replacement or data/settings change was performed.
User must normally quit before taking a fresh protected state/app backup and replacing the binary.
No staging or release backup was created yet. Recheck candidate hash/version and installed version
before resuming. Current user routing has changed since 0.58.65 installation: preserve it as-is.
Preview fixture/server were closed. Installed UI and live personal Reminders writes remain unverified.
