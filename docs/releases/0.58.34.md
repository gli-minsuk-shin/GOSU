# GOSU 0.58.34 — context capacity and token meter

Installed, 2026-09-13. See [context accounting](../CONTEXT_BUDGET_AND_USAGE.md) and
[release procedure](../RELEASE_RUNBOOK.md).

Includes native effective-capacity accounting, adaptive history/pressure compaction, preserved-history
retrieval, the compact assistant usage meter and bounded larger assistant tool budgets. No provider
connection, permission scope, API key, billing opt-in or user model preference is changed. Existing
histories remain in place. New optional checkpoint/usage fields require a compatible reader for rollback.

Source is the dirty working tree; no commit or push. One small synthetic Astra native call confirmed
828,400 effective context, 7,286 input / 15 output tokens; no private data or native tools were used.

## Release verification

- Final `pnpm check` passed: Desktop 2,397 passed / 8 existing environment skips (MacTeX opt-in 5,
  Linux-only 1, live Claude/Hermes 1 each); Briefing 723; Model Lab 341. Formatting, generated
  contracts, lint, typecheck and production builds passed.
- Named Agent Runtime 780 passed (288 Desktop + 491 Briefing + 1 Model Lab). The gate includes
  native metadata, transport/prompt, compaction, preserved-history and meter regressions.
- The branch-title test now counts catalog requests relative to setup: Auto discovers context
  metadata too, while the intentionally hung branch still times out correctly.
- Synthetic browser QA inspected collapsed and expanded meters. The one short live native probe
  above measured transport counters, not a 1M-input or general reasoning-quality benchmark.
- Directory package, fixed local app/helper signing, strict verification and isolated startup passed.
  Continuity reported preserved identity / no required one-time approval. Not public notarization.

## Installation

- Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`; installed: `/Applications/GOSU.app`.
- Candidate/staging/installed asar SHA-256:
  `f16655c00a45650f55b0dd7da138386f95ecc4b6f9347bab938d5722e13443c1`.
- Prior app backup:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.34-20260913-release.app`.
- Private state backup directory:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.34-20260913`.
  Contains byte-verified `workspace.v1.enc.json` and closed `gosu.db` copies; no WAL/SHM companions
  remained. Directory mode 0700, backup files 0600. Original files/permissions were not changed.
  Do not blindly roll back new data with an older snapshot or incompatible binary.
- No GOSU process was running before backup/replacement. No forced Quit, canceled user job or
  discarded draft. Verified staging `/Applications/.gosu-stage-0.58.34.23Rms6` was removed only
  after empty. Installed strict signature/hash/startup passed.
- Native accessibility and screenshot showed v0.58.34, restored prior AI conversation, and the
  token placeholder. Old replies lack metrics; none were fabricated. Populated meter QA used
  synthetic responses, and the native probe did not insert a message into the user's chat.
- Account connections, API keys, billing opt-ins, Keychain ACL and TCC settings were unchanged.
