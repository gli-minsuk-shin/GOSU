# GOSU 0.58.24 — candidate

Calendar direct human actions and semantic conflict checks. See [Briefing contract](../BRIEFING_WORKSPACE.md)
and [release procedure](../RELEASE_RUNBOOK.md).

The editor removes its extra review panel: Create/Save/Delete immediately prepare and apply one
scoped operation. Delete uses the observed event, not incomplete edited fields. Synchronous locks
prevent double clicks and repeated writes after an uncertain response.

Event fingerprints and native before-write comparisons ignore only modifiedAt (sync timestamp),
not actual event content. Real conflicts still stop execution; successful deletes remove the
observed entry. The screenshot's generic stale/change error is consistent with either failure;
tests reproduce the timestamp-only false conflict, not the exact user's private event.

Direct native execution requires both the UI direct flag and a verified, certificate-signed
installed GOSU parent matching its designated requirement and canonical app path. AI/default,
CLI, development and untrusted-parent invocations retain native confirmation. A boolean alone
cannot bypass the helper. No actual events or settings were changed.

## Verification

- `pnpm check` passed formatting, generated contracts, lint, typecheck, full workspace tests,
  root script checks and production builds. Desktop 2,380 passed / 8 environment skips;
  Briefing 660 passed; Model Lab 341 passed; UI 15 passed. Desktop skips: MacTeX opt-in (5),
  Linux-only (1), live Claude (1), live Hermes (1).
- Focused Calendar/editor regressions: 26 passed. Agent Runtime: 628 passed
  (184 Desktop, 443 Briefing, 1 Model Lab).
- Native Swift source typechecked successfully with the installed SDK; only pre-existing
  deprecated EventKit authorized-status warnings. This did not run Calendar operations.
- Synthetic real-render smoke: 8/8 create/delete/permission cases across 1000/460px widths.
  Create/delete each reached one mocked apply from one click. Both screenshots inspected.
- Directory packaging and isolated startup passed. Source .app signature passed the continuity
  command's validation phase, but continuity then correctly rejected the ad-hoc candidate.
  `app.asar` SHA-256: `2f2f80613d4730ab4b8b8cfd36e2233754e800ccbaa4d2a823e0e965fe0f2701`.

## Still blocked from deployment

The stable-signing gate rejects this development candidate. Installed GOSU remains 0.58.20;
no Quit/replacement, signing-credential setup, preference change or real Calendar deletion occurred.
The certificate-verified direct native path and the user's exact live failure remain unverified.
Do not describe synthetic apply results as actual calendar writes. Helper permission continuity
must also be verified during the future signed migration. Source is the dirty working tree at
`a4d5ef7effe0f863f799d646236f97b01898d94a`; no commit/push.
