# GOSU 0.58.37 — installed

Project Chat now shares the assistant's context usage meter, native accounting and pressure-based
conversation compaction. See [context architecture](../CONTEXT_BUDGET_AND_USAGE.md) and the
[release runbook](../RELEASE_RUNBOOK.md). Includes the earlier drag/drop candidate 0.58.36.

## Gates and observed behavior

- `pnpm check` passed: Desktop **2,409 passed / 8 existing environment skips**, Briefing **735**, Model
  Lab 341, Contracts 44, Briefing Core 116, UI 15, Integrations 19, Domain 14 and Sync API 51 passed.
  Typecheck, lint, formatting, generated contracts, root tests and production builds passed.
- Focused Project context/prompt/service/UI: **164 passed**.
- `pnpm test:agent-runtime`: **922 passed** (10 queue/drop + 418 Desktop + 493 Briefing + 1 Model Lab).
- Local SQLCipher smoke passed, including 310 backend vs 250 UI messages, original-message search
  paging/isolation, prefix-checkpoint validation and usage/checkpoint restoration after reopening.
- Synthetic production Project Chat UI inspected in the browser: collapsed one-line meter above the
  composer; expanded current capacity, remaining/reserves, cache/input/output and history counts.
- Package, fixed certificate, strict app/helper signatures and isolated startup passed. Continuity:
  `preservedIdentity: true`, `requiresOneTimeApproval: false`.
- Installed native UI showed **v0.58.37**, existing Project Chat history and model/reasoning selection,
  and `토큰 사용량 · 요청 후 표시`. Earlier turns are not backfilled with invented counters. New requests
  produce measured statistics. No test question, source mutation or paid live inference was inserted
  into the user's project; synthetic model responses do not prove a scientific-reasoning score gain.

Initial validation caught the old history-character schema ceiling and tests expecting prompt
provenance v6; guards/readers and focused tests were updated for v7 while retaining v1–v6 compatibility.
Native reports for the same model can supersede older fallback window metadata. Configured default
reasoning is preserved for the same-provider summary role; another provider is never selected silently.
The eight unchanged skips are opt-in/platform-dependent MacTeX, live-provider and Linux cases.

## Artifact and replacement

- Source: dirty Git HEAD `a4d5ef7effe0f863f799d646236f97b01898d94a`. No commit/push or unrelated reset.
- Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`; installed: `/Applications/GOSU.app`.
- asar SHA-256: `3ac3e768431de5ed8b841efe0e59dadc2bcd4d27aa39e7fc8cc7ce3c9b9e4597`.
- Signer: `GOSU Local Code Signing`, fingerprint `093F07514506D9D61282F362792B33741C834264`.
  Local fixed certificate, not public notarization; no OS trust or permission changes.
- Previous 0.58.35 backup:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.37-20260913-release.app`.
- Private-state backup:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.37-20260913`.
  Encrypted workspace **2,303,394 bytes**, SQLCipher DB **3,883,008 bytes**; byte-identical at backup,
  directory 0700/files 0600; no WAL/SHM. No private contents or keys were copied into these docs.
- GOSU was already closed before backup/swap. No user job was force-stopped. Verified staging hash and
  signatures, moved the old binary to backup, installed the candidate and removed only empty staging.
- The 0.58.36 drag/drop candidate was not separately installed; its changes are included here.
- Do not blindly roll an older binary back over newer DB provenance/context state. Preserve new data
  and use the documented recovery process with these backups if needed.

Logs under `/private/tmp`: `gosu-05837-final-check.log`, `gosu-05837-focused.log`,
`gosu-05837-db-smoke.log`, `gosu-05837-runtime.log`, `gosu-05837-package.log`,
`gosu-05837-sign.log`, `gosu-05837-continuity.log`. Markdown mirrors verified byte-identical.
