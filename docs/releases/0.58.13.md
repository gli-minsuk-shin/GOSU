# GOSU 0.58.13 — installed and sidebar verified

## Sidebar density follow-up (2026-09-11)

The installed build unifies personal/project/workspace rows at 34px minimum,
2px list gaps, 12px sidebar inset and a fixed 12px gap above local connections. Nested project
tabs use 30px minimum; group headers keep visible rounded keyboard focus. No labels, source data
or navigation actions are removed. Updated package hash/installation verification follows below.

Combined calendar/task briefings and approved read-only GOSU To-do access.
See [Briefing contract](../BRIEFING_WORKSPACE.md) and [release procedure](../RELEASE_RUNBOOK.md).

Implementation, full verification and installation complete. Actual installed version/header and
compact sidebar were inspected on the Appearance settings screen after a normal app restart.

## Scope and checks

- Calendar/To-do rows share the same briefing day columns and collapse together. Green schedule
  rows and purple labelled task rows distinguish content without extra top-level source sections.
  At most six overdue/upcoming/undated tasks are shown; completion/editing stays in GOSU Tasks.
- Optional Todo read grant defaults off for old profiles; enabling requires native Settings
  approval. AI task queries also require private-AI permission. Reader omits completed/archived
  tasks and inactive projects; source descriptions are not copied. No task write operation added.
- `pnpm check` passed: formatting, contracts, lint, types, workspace/root tests and builds.
  Desktop 2,373 passed / 8 existing environment skips; Briefing 618/618; Core 116/116.
  Other suites: Model Lab 341, Contracts 43, UI 15, Integrations 19, Domain 14, Sync API 51.
- Agent Runtime: 182 Desktop + 426 Briefing + 1 Model Lab = 609/609. Focused Todo/assistant/
  generation/cache tests 41/41. MacTeX/Linux/live-provider environment skips unchanged.
- Browser fixture inspected the combined two-column layout, labels/colors, task metadata and
  collapse with task count retained. No actual user task/Calendar data or paid inference used.
- Directory package: `apps/desktop/dist/mac-arm64/GOSU.app`, version 0.58.13; packaged startup
  verifier passed. Local ad-hoc signature, no notarization claim.
- Strict code-signature verification passed. app.asar SHA-256:
  `addf0942cd9e6fb63bdf57c98f0495bdaabb0fe375ed0376211ced8fd9b56b92`.
- Density regression 2/2 and sidebar visual smoke 4/4 light/dark + narrow/wide variants passed.
  Both light/narrow and dark/wide screenshots were inspected. Final installed screenshot confirms
  consistent menu spacing and more local-connection rows fitting on screen; font preferences retained.

## Installation and recovery

- Installed `/Applications/GOSU.app` is 0.58.13. Build/stage/installed app.asar hashes matched;
  strict signature and installed isolated-startup checks passed. Startup initially timed out in UI
  discovery, then the actual window opened and showed v0.58.13. No security dialog was manipulated.
- Previous 0.58.12 backup:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.13-20260911-150744.app`.
  Stage `/Applications/.gosu-stage-density-0.58.13.gZiMmM` removed only after empty.
- Old app was idle with an empty composer and exited through normal Quit before replacement.
  Project DB, Briefing stores, permissions and existing research data were not reset. No user
  task/calendar mutation or paid inference was run. Live To-do/Calendar data integration still
  requires the user's configured grants; this sidebar check does not claim those reads were run.
- Source HEAD remains `a4d5ef7effe0f863f799d646236f97b01898d94a` with accumulated uncommitted work;
  no commit/push. The earlier 0.58.10 backup remains as documented in [0.58.12](0.58.12.md).
