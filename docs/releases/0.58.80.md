# GOSU 0.58.80 installed

Shared tensor reasoning policy, structured graph narrative and canonical equation selection.
See [graph reasoning contract](../MODEL_GRAPH_PRESENTATION.md) and [release runbook](../RELEASE_RUNBOOK.md). Cumulative dirty source at
HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`; no commit/push.

Source generation, Copilot and full pseudocode normalization share the axis/operation policy.
New output requires purpose, equation index, shape explanation and uncertainty. Parser selects
the stored equation rather than trusting a duplicate; invalid annotations fail validation.
Cards show key math; the inspector shows complete reasoning. Pseudocode persistence retains the
annotations, and narrative-only repair discards stale annotations only on changed modules.
Cache namespace v9 avoids reusing old builder output as new-policy output. Existing models are not
rewritten and invalid old dimensions are not inferred from an earlier assistant explanation.

Synthetic hosted UI/restoration passed. One real Astra/high synthetic generation returned all five
module narratives in about 95 seconds. An earlier duplicate-equation attempt was rejected; the
index-based approach passed. This is not verification of the user's original damaged graph, nor
proof of arbitrary mathematical programs. No personal model/source/conversation was changed.

Focused regression and Model Lab 407 tests passed. Final full/Runtime/package/sign/install gates
pending. Current installed app is fixed-signed 0.58.79. User was asked to quit normally for safe
replacement; never force quit or reset settings/history.

## Final verification and installation

Full check passed: 4,161 tests, eight existing environment skips (MacTeX five, Linux one,
live Claude/Hermes one each); type/lint/format/contracts and production builds passed.
Final named Runtime passed 1,596 (119 + 816 + 545 + 116), including the narrative-repair regression.
Model Lab full suite 407 passed. Package and isolated startup passed. Fixed-certificate signing
and continuity gate passed: preservedIdentity true, requiresOneTimeApproval false.

Idle installed UI was checked; keyboard quit did not stop the app, but the native GOSU menu Quit
did. No forced termination. Fresh protected backup compared 76 files / 8,718,143 bytes:
`/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.80-NxNaxr`.
Previous app: `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.80-2026-09-14T12-29-26.345Z.app`.
Installed target: `/Applications/GOSU.app`; ASAR SHA-256
`bbb3d9df0b65455d661b2f0c4f4eb4343f33fdc65fe4bc96a7ea594879ef4116`.
Staged/installed version, hash and strict signature passed; empty staging removed.

Actual relaunch: initial UI lookup timed out; the next showed v0.58.80 and retained Project Chat
history. Model routing, approval policy, shortcut and language files compared unchanged after launch.
No private model was regenerated, no user graph revision was applied and no personal AI query was
sent during installation. Existing damaged graphs need a source-grounded reviewed regeneration;
installation alone does not validate their mathematics. Earlier pending statements describe preparation.
