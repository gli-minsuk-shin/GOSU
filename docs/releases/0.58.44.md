# GOSU 0.58.44 — verified candidate; normal shutdown pending

2026-09-14. [AI assistant recommendations](../GLOBAL_ASSISTANT.md) appear on opening and dismiss
on input clicks/typing, outside pointer presses or window/iframe focus loss. Automatic initial focus
keeps the list visible. List/toggle clicks are excluded from outside dismissal; suggestion clicks still
submit directly. Slow restoration never reopens an already-dismissed list. Existing messages, drafts,
permissions and model input are unchanged. Includes [0.58.43](0.58.43.md).

Superseded by [0.58.45](0.58.45.md), which includes these changes. The shared dist path now contains
0.58.45, not the historical 0.58.44 artifact below.

Focused recommendation/retention/immediate-submit tests: 32 passed. Agent Runtime: 1,010 passed
(30 + 430 + 510 + 40). Tests cover list/toggle exclusions, click/typing/blur dismissal, listener cleanup,
reopen, retained messages/drafts, delayed restoration and exactly-once suggestion execution.
Historical tests that assumed typing/reopen kept the old recommendation visibility were updated to
the new user-requested behavior; transcript preservation assertions remain.

Rendered the real BriefingChat component with synthetic restored history and a stubbed response.
Browser inspection confirmed initial automatic input focus with recommendations visible, outside/input
click dismissal, reopen, one-click submission and typing with the previous conversation retained.
No live model/source call; this is not installed-app verification. Closed the fixture browser/server
and removed its three temporary source/config files.
Installed app remains 0.58.40 awaiting normal shutdown; do not force quit or bypass macOS security UI.
No user data reset, source reads or live model calls. Follow the [release runbook](../RELEASE_RUNBOOK.md).

## Final gates and artifact

- `pnpm check`: Desktop 2,413 passed / 8 existing environment skips; Briefing 772; Model Lab 349;
  Briefing Core 116; Contracts 44; UI 15; Integrations 19; Domain 14; Sync API 51; root scripts 18.
  Total 3,811 passed. Formatting, generated contracts, lint, typecheck and production builds passed.
  Skips: five MacTeX opt-in, one Linux-only, live Claude and live Hermes opt-in.
- Agent Runtime 1,010 passed as above; document-link gate 2 passed.
- Dirty source at `a4d5ef7effe0f863f799d646236f97b01898d94a`, no commit/push.
- Candidate `apps/desktop/dist/mac-arm64/GOSU.app`, Info.plist 0.58.44.
  Asar SHA-256: `b65fb2ee21fc9eb8e958b13940d8100f12dab847d9f9bf960203e6f301481795`.
- Directory packaging, fixed-certificate strict app/helper verification and isolated startup passed.
  Signer `GOSU Local Code Signing`, fingerprint `093F07514506D9D61282F362792B33741C834264`.
  Continuity: `preservedIdentity=true`, `requiresOneTimeApproval=false`; not an OS-prompt guarantee.
- Final installed-process/version check still found 0.58.40 running. Normal shutdown/keychain prompt
  handling by the user is pending. No backup/swap for this candidate; existing protected backups are
  in [0.58.40](0.58.40.md). Installed UI is unverified; isolated startup does not read the user DB/keychain.

Logs under `/private/tmp`: `gosu-suggestions-focused-final.log`, `gosu-05844-check-final.log`,
`gosu-05844-runtime-final.log`, `gosu-05844-package.log`, `gosu-05844-sign.log`,
`gosu-05844-continuity.log`, `gosu-05844-docs.log`. Updated Markdown mirrors verified byte-identical.
