# GOSU 0.58.38 — installed; native UI confirmation blocked

2026-09-14. Adds [public-paper recovery](../PUBLIC_PAPER_RECOVERY.md) for AI assistant title/topic
search and original reading. See [source boundaries](../BRIEFING_LIVE_SOURCES.md) and the
[release runbook](../RELEASE_RUNBOOK.md).

## Verified gates

- `pnpm check` passed: Desktop **2,409 passed / 8 existing environment skips**, Briefing **744**, Model
  Lab 341, Contracts 44, Briefing Core 116, UI 15, Integrations 19, Domain 14 and Sync API 51 passed.
  Typecheck/lint/format, generated contracts, root tests and production builds passed.
- Focused lookup/assistant/public-source/native regression: **59 passed**.
- `pnpm test:agent-runtime`: **931 passed** (18 source/queue/drop + 418 Desktop + 494 Briefing + 1 Model Lab).
- Real public-source tests found both requested papers after an arXiv API failure: PMLR PDF text
  (60,000 characters, 15/24 pages) and DOI-verified arXiv HTML v1 (24,000 characters, 8 equations,
  4 figure references). The final fallback-only probe simulated an API cooldown to avoid further API
  calls; its PMLR/OpenReview/DataCite/article requests were real. About 2.0/2.1 seconds respectively.
  No full-paper-read or live LLM-summary-quality benchmark is claimed.
- OpenReview challenge redirects were classified and not followed/solved. No alternate IP, cookies,
  credentials or generic redirect following was used. Existing date filters remain for recent mode.
- Packaging, fixed-certificate strict app/helper verification and isolated installed startup passed.
  Signature continuity: `preservedIdentity: true`, `requiresOneTimeApproval: false`.

## Artifact and replacement

- Dirty source at Git HEAD `a4d5ef7effe0f863f799d646236f97b01898d94a`; no commit/push or unrelated reset.
- Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`; installed: `/Applications/GOSU.app`, version 0.58.38.
- Candidate/staging/installed asar SHA-256: `40047e0837fe96293ac9b8793a4c0563e1a036f809a1eb87e28ea22cfe105d98`.
- Signer: `GOSU Local Code Signing`, fingerprint `093F07514506D9D61282F362792B33741C834264`.
  Local fixed certificate, not public notarization.
- Old 0.58.37 app:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.38-20260914-release.app`.
- Private backup:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.38-20260914`.
  Encrypted workspace **2,305,878 bytes**, SQLCipher DB **3,895,296 bytes**, byte-identical at backup;
  directory 0700/files 0600; no WAL/SHM. No secrets or private user text copied into docs.
- The prior visible library was idle with no draft editing. Normal Quit via the native GOSU menu
  succeeded after a keyboard attempt did not. Confirmed no main process before backup/swap. Removed
  only the empty staging directory; original data, permissions and connection settings were not reset.

## Native UI remains unverified

Launching the installed binary left a running GOSU main process but native-window discovery timed
out. A SecurityAgent process appeared during launch, consistent with a macOS keychain/security approval
wait around database opening. Computer Use explicitly denied access to SecurityAgent, so the exact
prompt was not inspected and no approval, password entry, ACL/trust/TCC modification or workaround
was attempted. Asked the user to inspect and complete any displayed GOSU/keychain prompt themselves.
Cryptographic signature continuity does not prove the OS will never request keychain approval.

The binary is installed and isolated startup passed; do not claim installed UI or real user chat
execution was verified yet. Resume that check after the user's response. Keep the app/data backups.

Logs under `/private/tmp`: `gosu-05838-check.log`, `gosu-05838-focused.log`, `gosu-05838-runtime.log`,
`gosu-05838-package.log`, `gosu-05838-sign.log`, `gosu-05838-continuity.log`. Markdown mirrors verified.
