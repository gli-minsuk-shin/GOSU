# GOSU 0.58.36 — candidate

Superseded and included in installed [0.58.37](0.58.37.md). The pending status below is the original
candidate record; the mutable packaging directory now contains the newer artifact.

2026-09-13. Adds drag-and-drop attachments to AI assistant and Project Chat.
See [attachment boundaries](../CHAT_ATTACHMENTS_AND_QUEUE.md) and the [release runbook](../RELEASE_RUNBOOK.md).
No automatic message send, model call or original-file modification on drop. Existing drafts, queues,
settings and conversations are preserved. Native Files use preload path resolution; Briefing redeems
short-lived, single-use tickets only after checking the current authenticated routine.

## Verified candidate

- `pnpm check` passed: Desktop 2,403 passed / 8 existing environment skips; Briefing 734 passed;
  Model Lab 341, Contracts 44, Briefing Core 116, UI 15, Integrations 19, Domain 14, Sync API 51.
  Typecheck, lint, formatting, generated contracts, root tests and production builds passed.
- Focused Desktop drop/bridge/service/IPC regression: 19 passed; Briefing drop/queue/host: 6 passed.
- `pnpm test:agent-runtime`: 915 passed (10 new queue/drop group + 412 Desktop + 492 Briefing + 1 Model Lab).
- Native regression `apps/desktop/test/electron/chat-drop-bridge-smoke.cjs` passed using only a
  temporary disk-backed text fixture in a hidden isolated Electron window. The File made a round trip
  through the loopback iframe and the file-origin parent, then resolved in the isolated preload.
  This validates backing-file preservation across the real boundary, not a live model invocation.
- Synthetic production-component UI inspected with computer-use tools: nested drag highlight and
  target notice. Unit UI regression confirms drop attaches without auto-send and preserves draft.
- Package and fixed-certificate signing passed. Strict app/helper validation and isolated startup passed.
  Continuity: `preservedIdentity: true`, `requiresOneTimeApproval: false`.

Source: dirty Git HEAD `a4d5ef7effe0f863f799d646236f97b01898d94a`; no commit/push or unrelated reset.
Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, version 0.58.36.
asar SHA-256: `ea2860b61cec0803141e57dff0693f72bdc3a775ededaaabb5eaeb0281489a94`.
Signer: `GOSU Local Code Signing`, fingerprint `093F07514506D9D61282F362792B33741C834264`;
local certificate, not public notarization. No OS trust or permission settings changed.

## Installation pending — active user work

Installed app remains `/Applications/GOSU.app` version **0.58.35**. Native UI showed an active Project
Chat (Stop/queue controls), so it was not interrupted or replaced. Asked the user to quit normally
after their work completes. No backup/swap for 0.58.36 has yet been performed, and no installed-UI
success is claimed for this candidate. After shutdown, recheck scope/hash/signature and follow the
release runbook, preserving user data and taking a fresh backup before replacement.

Logs under `/private/tmp`: `gosu-05836-check.log`, `gosu-05836-focused.log`,
`gosu-05836-briefing-focused.log`, `gosu-05836-native-drop.log`, `gosu-05836-runtime.log`,
`gosu-05836-package.log`, `gosu-05836-sign.log`, `gosu-05836-continuity.log`, `gosu-05836-format.log`.
Markdown source/mirror parity and maintenance links verified.
