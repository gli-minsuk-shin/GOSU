# GOSU 0.58.45 — verified extension-request candidate; actual 1M unconfirmed

2026-09-14. See [context capacity](../CONTEXT_BUDGET_AND_USAGE.md). Includes [0.58.44](0.58.44.md).

Superseded by [0.58.46](0.58.46.md), which includes these changes. The shared dist path now contains
0.58.46; the 0.58.45 artifact hash below is historical verification evidence.

- Astra requests the documented 1,050,000-token window even when native cache metadata is smaller.
- Other models use their own discovered maximum/default/percentage. Exact model-ID/wire-alias matching,
  fresh bounded cache reads, provider catalog discovery and unknown fallback remain explicit.
- Common meter distinguishes requested, detected maximum, default and actual reported capacity.
- AI Assistant, Project Chat and Model Lab learn capacity only for matching model/context settings;
  stale limits do not pin new configurations. Original conversations/settings/permissions stay intact.
- Two tiny synthetic live probes; final result: request 1,050,000, detected max 872,000, reported
  effective 828,400; input 7,256, output 5, cached 0. No private content/tools. Actual 1M execution is
  still blocked by the tested transport, not declared successful or bypassed with forged metadata.
- No global config/auth change, API billing fallback, source reads or user data reset.

Focused contracts 45 and context/meter 7 passed; focused Desktop transport/context tests 175 passed
before adding one more compaction-safety regression. Final gates below include that additional test.
Installed app remains 0.58.40 awaiting normal shutdown/keychain handling. Follow the
[release runbook](../RELEASE_RUNBOOK.md).

## Final evidence

- `pnpm check`: Desktop 2,414 passed / 8 existing environment skips; Briefing 773; Model Lab 349;
  Contracts 45; Briefing Core 116; UI 15; Integrations 19; Domain 14; Sync API 51; root scripts 18.
  Total 3,814 passed. Formatting, contracts check, lint, typecheck and production builds passed.
  Skips: five MacTeX opt-in, one Linux-only, live Claude and live Hermes opt-in.
- `pnpm test:agent-runtime`: 1,012 passed (30 + 431 + 511 + 40).
- Verified real ContextUsageMeter rendering in a local synthetic HTML fixture: collapsed summary uses
  effective runtime capacity; expanded details distinguish 1,050,000 requested / 872,000 native max /
  272,000 default / 828,400 applied. Browser and local server closed, temporary test source removed.
  This is component rendering, not installed UI or a 1M-token recall benchmark.
- After the live capacity probe, a regression-tested safety adjustment keeps auto-compaction below
  the detected execution envelope (704,140 for 828,400), even while requesting extension. No further
  live inference was required for that conservative threshold adjustment. No telemetry is fabricated.
- Claude's existing model-matched runtime usage detection remains intact; no live Claude invocation.

## Candidate and installation blocker

- Dirty source at `a4d5ef7effe0f863f799d646236f97b01898d94a`; no commit/push.
- Candidate `apps/desktop/dist/mac-arm64/GOSU.app`, Info.plist 0.58.45.
- Asar SHA-256: `a9ce1bb4591d1227aafeb42e175c1f53dc01c2b14943bbd76c290461c48d9bd5`.
- Directory package, fixed-certificate strict app/helper verification and isolated startup passed.
  Signer `GOSU Local Code Signing`, fingerprint `093F07514506D9D61282F362792B33741C834264`.
  Continuity `preservedIdentity=true`, `requiresOneTimeApproval=false`; not an OS-dialog guarantee.
- Installed `/Applications/GOSU.app` remains 0.58.40 with the previously observed keychain-waiting
  main process still running. No forced termination, OS-security workaround or new backup/swap.
  Existing protected backups are recorded in [0.58.40](0.58.40.md). Installed UI is unverified.
- Actual 1M execution is not confirmed: the tested Codex runtime/connection reports the smaller
  effective window. This observation does not establish the API service's hard limit or account tier.

Logs under `/private/tmp`: `gosu-context-live-probe-final.log`, `gosu-context-desktop-final.log`,
`gosu-context-contracts.log`, `gosu-context-focused.log`, `gosu-05845-check-verified.log`,
`gosu-05845-runtime-final.log`, `gosu-05845-package-final.log`, `gosu-05845-sign-final.log`,
`gosu-05845-continuity.log`. Updated Markdown mirrors verified byte-identical.
