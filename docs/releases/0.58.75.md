# GOSU 0.58.75 candidate

[Prepared email actions](../EMAIL_PREPARED_ACTIONS.md), following the [release runbook](../RELEASE_RUNBOOK.md).
Includes the uninstalled 0.58.74 web/media change. Source HEAD
`303360b9b729d65bf568f79da3fbe1a137ccb67c`, cumulative dirty worktree; no commit/push.
Focused tests: 42 passed before the extra legacy-calendar regression. One synthetic live Luna/low call
prepared a correct meeting title/time/location and a distinct task deadline without a second call.
`pnpm check`: 4,114 passed, 8 existing environment skips (MacTeX 5, Linux 1, live Claude/Hermes 1
each); format/contracts/lint/typecheck/production build passed. `pnpm test:agent-runtime`: 1,487 passed
(100 + 794 + 539 + 54). Additional fixture typecheck/lint/format passed.
Browser inspection confirmed populated task/date/notes, an enabled local-save button without Apple
permission, and populated event title/start/end/location without draft endpoint/LLM calls. The fixture
prohibited all writes. Closed the preview tab/server after inspection.

Packaging initially hit an ENOTEMPTY race caused by a Finder `.DS_Store` in the otherwise empty output
directory. Moved only that build-metadata directory aside to `apps/desktop/dist/mac-arm64-finder-metadata-05875`
and retried packaging from the already-verified production build. Packaged isolated startup passed.
Native UI lookups timed out twice; no forced quit or replacement was attempted by this task.
User normal exit is needed before fresh backup and installation. No user state was reset.

## Signing gate blocked by externally changed installed app

Prepared candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, 0.58.75,
ASAR SHA-256 `e2d7e9312076ccb7e5738d918193cdc264b271deb2c46ce44e6ae01ff212d5ac`.
Fixed signer `093F07514506D9D61282F362792B33741C834264`; app/helper strict signature verification passed.
The update-continuity gate then failed: `/Applications/GOSU.app` had changed during this task to
0.58.75 with an ad-hoc designated requirement (`cdhash ef9b831122eee0dbdc7a27211cbb271e99d0fe99`),
instead of the previously observed certificate-signed 0.58.73. This task did not copy/move anything
into `/Applications/GOSU.app`. Do not describe this externally changed app as our verified installation.
Its ASAR hash matches the prepared candidate; the mismatch is the signing identity, not proof that
the installed process has loaded/restarted into this code or that OS permissions work.
Do not bypass the gate or silently change Keychain/TCC. Obtain explicit user review for restoring the
fixed certificate identity (possible one-time OS approvals), then normal exit, fresh backup, rerun the
reviewed migration gate, and verify actual installed UI. No installation backup was made by this task.
