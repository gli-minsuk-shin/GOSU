# GOSU 0.58.84 candidate

See [chat execution continuity](../CHAT_EXECUTION_CONTINUITY.md) and
[release procedure](../RELEASE_RUNBOOK.md). Cumulative dirty source; no commit/push.

Model Copilot pending runs/progress are keyed by model/version/revision instead of active view.
Navigation does not cancel or discard answers; completion targets the captured conversation.
Explicit stop, trash, disposal and permission boundaries remain. Delayed proposals do not overwrite
newer drafts/revisions. Main-window background throttling is disabled for retained chat delivery.
Focused tests cover multiple sessions, duplicate send, hidden completion, stop isolation, deletion,
progress bounds and stale completion, plus existing global-assistant and Project Chat ownership.
Navigation-triggered SSH cancellation/revocation and automatic background denial are removed;
global pending approvals retain their owning scope and still require explicit approval where needed.

## Verified candidate; installation pending normal quit

Full check passed: 4,204 tests / eight existing environment skips (MacTeX five, Linux-only one,
live Claude/Hermes one each). Model Lab 412, Briefing 915, Desktop 2,599, other packages 260,
root scripts 18. Named Runtime gate 1,683 passed (14 + 157 + 819 + 563 + 130; some suites are
intentionally exercised twice). Typecheck/lint/format/contracts and production builds passed.
The initial concurrent check hit the existing 5-second encrypted-history pagination test timeout;
no test was disabled or relaxed, and the final full rerun passed that case in approximately 2.1s.
React-render regression exercised hidden/switched sessions and restored original completion;
no private model question, graph mutation, SSH command or live LLM invocation was performed for QA.

Packaged artifact: `apps/desktop/dist/mac-arm64/GOSU.app`, 0.58.84.
ASAR SHA-256: `fa7e951b717e4c8d7c5f605063c7850f97d6d1c195e41e2ac7e7a57dac4a167f`.
Fixed certificate `093F07514506D9D61282F362792B33741C834264`, strict app/helper checks,
packaged startup smoke and update continuity passed. No identity migration or new OS permission.

Actual UI inspection confirmed the installed 0.58.83 now runs, so the preceding keychain wait
had cleared. This does not prove every hidden AI task is idle. The app was not automatically quit
or replaced, to avoid interrupting background work while fixing this exact issue. No 0.58.84
installation backup or swap has occurred. After the user finishes work and quits normally,
recheck inactivity, take a fresh protected data/binary backup, reverify this artifact/signing and
install; then verify the actual installed UI. Installed version remains 0.58.83.
