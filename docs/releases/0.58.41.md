# GOSU 0.58.41 — verified candidate; app shutdown pending

Superseded by the verified [0.58.42 candidate](0.58.42.md), which includes the Model Lab changes.
0.58.41 was not installed; the candidate path below now holds the newer artifact. Check hashes.

2026-09-14. [Model Lab context parity](../MODEL_LAB_CONTEXT.md) applies the shared
[context/usage system](../CONTEXT_BUDGET_AND_USAGE.md) to Model Copilot chat and revision review.
Follow the [release runbook](../RELEASE_RUNBOOK.md). Installed app is still 0.58.40.

Separate scoped originals/checkpoints, same-provider pressure compaction, original-text search,
native occupancy/unknown counters, durable usage and the shared compact meter are implemented.
Existing model-lineage permanent memory, selected models and review-before-apply are preserved.
Claude tool namespace registration now matches its Desktop adapter. No live model call or private
user conversation was used for QA. Old pruned messages are not reconstructed.

## Verification

- Focused context/native/runtime/storage: 39 passed. Includes 600 originals, a 90k-character record's
  deep continuation, restart, pressure/checkpoint reuse, corruption/cancellation, namespace isolation,
  native occupancy/compaction and restored telemetry.
- Final `pnpm check`: Model Lab 349, Briefing 761, Desktop 2,409 passed / 8 existing environment skips;
  Contracts 44, Briefing Core 116, UI 15, Integrations 19, Domain 14, Sync API 51; root tests 18.
  Typecheck, lint, formatting, generated contracts and production builds passed. The initial lint
  failure for missing error cause was corrected before the final run.
  Desktop skips: five MacTeX opt-in, one Linux-only, live Claude and live Hermes opt-in.
- `pnpm test:agent-runtime`: 987 passed (30 + 418 + 499 + 40). The named gate now explicitly includes
  Model Lab context, native transport, runtime adapter and persistence regressions.
- Isolated browser preview used the actual Model Lab composer/styles and shared meter with clearly
  synthetic telemetry. Inspected collapsed and expanded layouts: compact single line above input,
  detailed capacity/cache/compaction information only after expansion. No user workspace or LLM call.
  Removed the temporary fixture and closed its tab/server after inspection. This is component-level
  real rendering, not a claim of installed Model Lab UI or live model-quality verification.

## Candidate and installation boundary

Source is dirty at `a4d5ef7effe0f863f799d646236f97b01898d94a`; no commit/push.

- Candidate `apps/desktop/dist/mac-arm64/GOSU.app`, Info.plist 0.58.41.
- Asar SHA-256: `34c7c5070eb9e14343a60335e5447609d3f994805576962cf3629f154513a8fd`.
- Directory package, fixed-certificate strict app/helper verification and isolated packaged startup
  passed. Signer `GOSU Local Code Signing`, fingerprint `093F07514506D9D61282F362792B33741C834264`.
  Continuity returned `preservedIdentity=true`, `requiresOneTimeApproval=false`; this does not guarantee
  that macOS will never show a keychain prompt.
- **Not installed.** `/Applications/GOSU.app` remains 0.58.40 with its main process still running.
  Native window lookup timed out. Asked the user to handle any macOS approval directly and quit GOSU.
  No SecurityAgent interaction, forced stop, trust/ACL/TCC changes, app swap or data reset was performed.
- No new backup/swap for 0.58.41 yet. After normal shutdown, follow the runbook with a fresh data/app
  backup and verify the exact candidate hash. Existing backups are listed in [0.58.40](0.58.40.md).

Logs under `/private/tmp`: `gosu-model-context-focused.log`, `gosu-05841-check-verified.log`,
`gosu-05841-runtime-final.log`, `gosu-05841-package.log`, `gosu-05841-sign.log`,
`gosu-05841-continuity.log`. Markdown mirrors verified.
