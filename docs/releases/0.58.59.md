# GOSU 0.58.59 candidate

See [Briefing evidence](../BRIEFING_INTELLIGENCE.md#inference-and-evidence-contracts) and
[release procedure](../RELEASE_RUNBOOK.md).

Scholar alert discovery no longer depends only on the incrementally collected email summary list.
A separate bounded targeted Mail read uses the exact saved scope and no summary exclusion plan.
Successful alert papers survive ordinary Mail/public-source failure; final scope checks, paper
deduplication and private-AI permission boundaries remain. Crossref now sorts candidates by relevance
within the existing publication-date range instead of taking the newest potentially unrelated hits.

Read-only live diagnosis with current saved settings: public lookup returned 2 eligible candidates;
Crossref and OpenReview responded. Targeted approved Mail lookup took about 13 seconds and returned
10 alerts yielding 12 paper candidates. Only counts/timing are recorded here. No Mail status, settings,
history, or model invocation was changed by these diagnostics. Candidate extraction is not proof of
completed AI summaries. Focused regression tests: 32 passed.

## Gates

- `pnpm check`: 4,003 passed (3,985 workspace + 18 root), 8 existing environment skips
  (MacTeX opt-in 5, Linux-only 1, live Claude/Hermes opt-in 1 each). Briefing 810, Desktop
  2,557, Model Lab 358. Format, generated contracts, lint, typecheck and production build passed.
- `pnpm test:agent-runtime`: 1,421 passed (74 + 772 + 527 + 48).
- The first Briefing suite found an old assertion assuming a single pre-revocation Mail read.
  That test now clears pre-revocation calls and asserts no further read after revocation;
  the full suite subsequently passed. No permission assertion was removed.
- Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`, including existing dirty work.
  No commit/push. Installed app remains 0.58.58 with an active briefing generation;
  it has not been interrupted or replaced.

## Prepared installation

- Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, version 0.58.59.
- app.asar SHA-256: `ba94c0baeb55f8a47c56a2ebf0cc76936da4ad18f5ccaf7cd508d57d5f4e1ea8`.
- Packaged isolated startup, fixed-certificate app/helper strict signing and continuity passed:
  preservedIdentity=true/requiresOneTimeApproval=false. No OS security policy was changed.
- Native UI continued to show disabled Generate and active Stop generation at the final check.
  No force quit, data overwrite or app replacement. User normal termination is requested before
  taking a fresh state/app backup and performing the release-runbook swap. Installed end-to-end
  AI summarization is not yet verified; live evidence above is discovery/extraction only.
