# GOSU 0.58.25 — local signing migration candidate

Follow the [release runbook](../RELEASE_RUNBOOK.md) before any app replacement.
See the [maintenance guide](../MAINTENANCE_GUIDE.md) for current installation status.

Status: source candidate, not installed. Existing installed app remains 0.58.20.
The user approved creation/installation of a local signing identity and the initial OS approval
transition on 2026-09-12. Certificate Assistant is prepared; completion is not yet verified.
No signing key is exported into this repository or documentation.

Includes generation-relative New badges and exact Calendar links from History/live summary cards.
The Calendar helper is compiled into the app before signing and uses a fixed bundle location;
packaged runs must not silently rebuild an ad-hoc helper. Development retains its existing helper.
This is a local self-signed identity, not Developer ID, notarization or an Internet release.

The local development package and isolated startup check passed. Its temporary ad-hoc signature
is valid on disk but does not satisfy the update continuity policy; it was not installed.
Artifact: `apps/desktop/dist/mac-arm64/GOSU.app`.
`app.asar` SHA-256: `c74bc1d11182deb6293ea2de4e5c2627bf22b58797d7539cc182d771d406d4ec`.
Focused tests: 9 Calendar tests plus 1 packaging-source test passed. Agent Runtime: 629 passed
(184 Desktop, 444 Briefing, 1 Model Lab). `pnpm check` passed formatting, generated contracts,
lint, typecheck, full workspace tests, root script checks and production builds. Briefing 662,
Desktop 2,380, Model Lab 341 passed; Desktop retained 8 environment skips (MacTeX opt-in 5,
Linux-only 1, live Claude/Hermes 1 each). Interactive visual verification is blocked by Mac lock.
Native packaged helper compilation and signature inspection passed; helper is currently ad-hoc
too and must be signed/verified using the same fixed certificate before installation.

Certificate Assistant was prepared with `GOSU Local Code Signing`, `Code Signing`,
`Self Signed Root`; the final Create step was handed to the user. Last identity inspection still
found zero identities. Subsequent UI access reported that the Mac was locked, so user unlock and
certificate completion are required. No private key creation/export is claimed.
Fixed-signed artifact, identity continuity and installed UI verification remain pending.
No user data, Calendar events, mail, TCC database or existing Keychain access controls were changed.
