# GOSU 0.58.76 candidate

Notification-to-Briefing scroll repair; see [notification contract](../WORKSPACE_NOTIFICATIONS.md)
and [release runbook](../RELEASE_RUNBOOK.md). Cumulative dirty tree at
`303360b9b729d65bf568f79da3fbe1a137ccb67c`; no commit/push.

The notification branch used scrollIntoView, unlike ordinary scoped Briefing jumps. A real browser
fixture with the production Briefing app and host CSS reproduced document scrollTop=55, missing
top heading and an equal bottom gap. The fixed path uses only `.briefing-main-scroll`, retains
preventScroll focus, and restores the embedded document/owned desktop host offsets to zero on
navigation. It waits two animation frames for visibility/layout and does not replay a handled alert
when history refreshes. Missing targets can retry after loading; cleanup cancels queued jumps.
Ordinary tab restoration is skipped while a notification target owns the requested position.
No iframe reload, history reset, content fetch/AI action, or account permission change is introduced.

Focused tests: Briefing 23, Desktop 4 passed. Browser screenshots confirmed the fixed document offset
at zero with the full 656px frame height; the old path reproduced the 55px gap.
After notification navigation, an additional 300px inner scroll remained in place (2192→2492),
with document/host offsets zero. Hiding the retained iframe and returning through the notification
also restored the intended record and full height. The preview tab/server were closed after QA.
`pnpm check`: 4,118 passed, 8 existing environment skips (MacTeX 5, Linux 1, live Claude/Hermes 1
each); format/contracts/lint/typecheck/production build passed. Runtime gate: 1,496 passed
(108 + 795 + 539 + 54). Final visual-fixture typecheck/lint/format passed separately.

## Prepared, installation blocked

Candidate `apps/desktop/dist/mac-arm64/GOSU.app` 0.58.76 passed packaged isolated startup and strict
app/helper signature verification using certificate `093F07514506D9D61282F362792B33741C834264`.
ASAR SHA-256: `291c9f060a9d648493227f22a7f361f5a70880cf18e18868967feb538653501f`.
The continuity gate failed versus current `/Applications/GOSU.app` 0.58.75, the previously observed
ad-hoc installation. No target copy, app shutdown, state backup or permission change was performed
by this task. Obtain explicit user review of restoring the fixed signing identity (possible OS
reapproval), then normal exit, fresh backup and the reviewed migration verification before installing.
The browser reproduction/verification is not a claim that the currently installed app is fixed.
