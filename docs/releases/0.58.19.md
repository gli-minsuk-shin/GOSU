# GOSU 0.58.19 — candidate

Mail-to-Calendar review and conservative verified cross-account deduplication. Includes the pending
0.58.18 native-calendar-ID fix. See [Briefing contract](../BRIEFING_WORKSPACE.md) and
[release procedure](../RELEASE_RUNBOOK.md).

## Verification

- Final `pnpm check` passed formatting, generated contracts, lint, typecheck, full workspace tests,
  root script checks and production builds. Desktop: 2,380 passed / 8 environment skips;
  Briefing: 643 passed; Model Lab: 341 passed; UI: 15 passed. Desktop skips: MacTeX opt-in (5),
  Linux-only (1), live Claude (1), live Hermes (1).
- Named Agent Runtime: 621 passed (184 Desktop, 436 Briefing, 1 Model Lab).
- Focused mail/date/UI set: 40 passed. Native-reader simulation/multi-account/store integration:
  23 passed. Tests cover preserved recipient identities, encrypted reload and exclusion of each
  summarized copy, source proof without raw-source output, rejected attachment/multipart proof,
  different tails/headers/previews, no title-only merging and no write on opening review.
- Synthetic Electron Calendar review: 2/2 widths (1000/460). Production styles were loaded and
  screenshots inspected. Explicit review/apply performed once against mocked routes only.
- Directory package, deep/strict signature and isolated packaged startup passed for
  `apps/desktop/dist/mac-arm64/GOSU.app` version 0.58.19. Local ad-hoc signature, not notarized.
  `app.asar` SHA-256: `4324f1c69241e1a80236aee905715bb7148993d97937d889df2100767c077a82`.

## Deployment boundary

Installed app is still 0.58.17; window inspection/replacement remains blocked until it is closed.
No real Mail/Calendar reads, source grants or calendar writes were exercised for this change.
This candidate includes the pending 0.58.18 History native-ID correction. Existing summaries
without content proof are preserved rather than guessed duplicates. Multipart/attachments are
intentionally separate until a complete verifier exists. Raw source is never sent to the LLM.
Dirty working tree at `a4d5ef7effe0f863f799d646236f97b01898d94a`; no commit/push.
