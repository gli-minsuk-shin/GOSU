# GOSU 0.58.22 — candidate

Email content-first generation, priority-only answer rejection and targeted cache/ingestion repair.
Includes the pending 0.58.21 Calendar create/access fixes. See [Briefing contract](../BRIEFING_WORKSPACE.md)
and [release procedure](../RELEASE_RUNBOOK.md).

## Verification

- `pnpm check` passed format, generated contracts, lint, typecheck, workspace tests, root script
  checks and production builds. Desktop 2,380 passed / 8 environment skips; Model Lab 341 passed;
  UI 15 passed. Final Briefing full suite: 655 passed. Desktop skips: MacTeX opt-in (5),
  Linux-only (1), live Claude (1), live Hermes (1).
- Focused quality/generation/store set: 18 passed. Tests cover one bounded correction, meaningful
  content with uncertain importance, latest corrected history stopping repair, ingestion and
  daily-completion readmission. Agent Runtime: 625 passed (184 Desktop, 440 Briefing, 1 Model Lab).
- Directory package, deep/strict signature and isolated startup passed for
  `apps/desktop/dist/mac-arm64/GOSU.app` version 0.58.22, local ad-hoc/not notarized.
  `app.asar` SHA-256: `b08a830a7b2064a22b3fd74016150103c7ad7fee46e95d545f787b916f245c81`.

## Actual configuration finding and pending authority

Read-only configuration inspection returned mail configured, mailRead=false, mailAi=true,
mail/body preview=false, assistant body preview=false. Provider is Codex/default model; scope
is three days/up to one hundred messages. No account names, message contents or secrets were logged.
The user was asked to authorize reading up to 4,000 characters per message and sending those previews
to the configured Codex model. That confirmation had not arrived; no source permissions/settings
were changed and no real model inference was performed.

Installed app remains 0.58.20. Do not claim the new generation policy has been applied to actual
messages. This candidate includes the pending 0.58.21 Calendar fixes. Good metadata-only summaries
are not automatically upgraded to full-body summaries by the priority-boilerplate repair alone;
body availability and user authorization remain separate prerequisites.
Source is the dirty tree at `a4d5ef7effe0f863f799d646236f97b01898d94a`; no commit/push.
