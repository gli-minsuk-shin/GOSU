# GOSU 0.58.87 candidate

See [generation deadlines](../MODEL_GENERATION_DEADLINES.md) and
[release procedure](../RELEASE_RUNBOOK.md). Includes pending 0.58.86 activity stars.
Cumulative dirty source; no commit/push.

Local failure inspection found a 300-second process cutoff before any candidate. Graph provider
calls now use bounded 15/30-minute deadlines consistently across import and compilation. Other
request kinds and user model settings remain unchanged. An explicit import Stop action propagates
cancellation, closes readers and rejects late results while preserving completed imports/history.

Focused verification: 81 passed. Full check passed: 4,239 tests / eight existing environment skips
(MacTeX five, Linux-only one, live Claude/Hermes one each). Model Lab 431, Briefing 917,
Desktop 2,605, UI 23, other packages 245, root scripts 18. Runtime 1,780 passed
(8 + 17 + 159 + 822 + 563 + 211). Typecheck/lint/format/contracts and production builds passed.
Actual ModelLabApp synthetic UI loaded a delayed import stream, exposed Stop, transitioned to
STOPPED when clicked and retained its original one-model workspace. Provider calls were mocked.
This is not a claim that a real provider completed the reported source under the new deadline.

Packaged with existing local Electron 43.2.0 through electronDist. Runtime preparation, packaged
startup smoke, fixed certificate signing and strict app/helper checks passed. Update continuity
preservedIdentity=true, requiresOneTimeApproval=false.
Candidate: `apps/desktop/dist/mac-arm64/GOSU.app`, 0.58.87.
ASAR SHA-256: `97826f1f4deaeb68805ac0384e85f7bcd83c2f777fba2f75216c26fb6a82a597`.
Certificate: `093F07514506D9D61282F362792B33741C834264`.

Installed version remains 0.58.85. Its process was still present after the preceding keychain wait;
normal shutdown has not been confirmed. No forced quit, 0.58.87 backup, swap or installed UI check
was performed. New limits do not affect that still-installed old binary until replacement. After
the user handles the OS prompt and quits normally, take a fresh backup and perform the release gates.
No actual user model or source was regenerated or modified during diagnosis. Do not force-quit
the installed app or change keychain permissions to install this update.
