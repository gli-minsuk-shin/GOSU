# GOSU 0.58.69 candidate

See [Model Lab integration](../MODEL_LAB_DESKTOP.md) and [release runbook](../RELEASE_RUNBOOK.md).
Adds optional `usage.modelExtraction` to model routing: fast, strong or existing. Old policies load
unchanged; absence means existing Model Lab selection. Explicit extraction roles override the chat
selection only for source-to-ModelIR reconstruction, including its audit repairs. An unconfigured
role fails visibly rather than silently choosing a different model. Codex/Claude Code are supported.

Canonical source cache reuse still avoids any LLM call and reports its original model. Identical
in-flight project/source builds are still joined without duplicating calls or changing the active
model; role changes apply to subsequent new LLM calls. Saved models and chat selections remain untouched.

## Verification

- Focused Model Lab: 51 passed (routing, canonical cache and copilot server suites); Desktop
  routing/settings: 5 passed. Covers fast/strong override, existing/legacy preservation, empty-role
  rejection, supported providers, stored selection, localized errors and existing cache reuse.
- `pnpm check`: 4,066 passed (4,048 workspace + 18 root), 8 existing environment skips
  (MacTeX opt-in 5, Linux-only 1, live Claude/Hermes 1 each). Desktop 2,583, Briefing 841,
  Model Lab 364. Format/contracts/lint/typecheck/production builds passed.
- `pnpm test:agent-runtime`: 1,458 passed (80 + 786 + 538 + 54); new extraction routing
  regressions are part of this named gate.
- Browser fixture rendered the new usage row with fast/strong/existing options in the existing
  settings card. Unit tests exercised changing/saving the role; no personal model extraction was
  invoked and no existing user's routing policy was rewritten.
- Source HEAD `303360b9b729d65bf568f79da3fbe1a137ccb67c`, existing dirty changes preserved,
  no commit/push. 0.58.67 shortcut and 0.58.68 personal-task changes are included.
- Old apps do not recognize the new optional routing field after it is saved; keep the protected
  pre-update model-routing file with any rollback, rather than resetting unrelated settings.

Installation remains pending normal GOSU quit; current installed version is 0.58.66.

## Final artifact; not installed

Candidate: `apps/desktop/dist/mac-arm64/GOSU.app` 0.58.69. Final ASAR SHA-256:
`d0aea1ea50481996cf2e0e78d80f444ae80eb6552af1802c636eb1eee148b781`.
The final build preserves source-only singleflight reuse. Full and named regression gates were rerun
after that decision and passed with the counts above. Packaging, isolated startup, fixed-certificate
app/helper strict signing and update continuity passed (`preservedIdentity=true`,
`requiresOneTimeApproval=false`). Preview fixture/server were closed; Markdown mirrors verified.

GOSU 0.58.66 is still running; no 0.58.69 backup/staging/replacement was performed. Prior current-app
UI lookups timed out, and normal quit has not been confirmed. Do not overwrite or force-quit an
unknown active job. After user quit, recheck version/hash, take fresh protected app/state backups
including current model routing, then follow the runbook. Installed settings UI and a user's live
extraction remain unverified. Existing routing/history/model files were not modified by this task.
