# GOSU 0.58.51 installed

See [sidebar design](../SIDEBAR_ICON_DESIGN.md) and the [release runbook](../RELEASE_RUNBOOK.md).

Supersedes the rejected, uninstalled 0.58.50 design. The assistant drawing grows to 28px while its
alignment slot stays 22px and the button stays unchanged. A full 20×18-unit speech bubble and three
dots replace the squeezed bubble. A 12-unit green sparkle overlays the upper-right edge with a
theme-surface separator. At actual size the bubble is about 23×21px and sparkle 14px, versus the
previous candidate's roughly 9×10px bubble and 8px sparkle. Other navigation graphics remain 18px.
No chat panel, text size, behavior, permission, provider or data-storage change is included.

## Verification

- Focused sidebar tests: **19 passed**. The full-size bubble, three dots, green badge/halo and
  assistant-only size override are protected. The old exact wrapper-class assertion was updated
  to allow the assistant modifier without losing coverage of any icon.
- Native production-renderer visual smoke: **4/4 variants passed**, light/dark at 260/332px
  sidebar widths. Full and button-only screenshots were inspected. Tests require a 20×18-unit
  bubble, 12-unit sparkle, 28px assistant graphic, unchanged 22px centered slots, 18px other icons,
  room for the badge halo inside the button, no horizontal overflow and working navigation.
- `pnpm check`: **3,954 passed / 8 existing environment skips**. Desktop 2,547; Model Lab 356;
  Briefing 773; Contracts 45; Briefing Core 116; UI 15; Integrations 19; Domain 14; Sync API 51;
  root scripts 18. Format, generated-contract checks, lint, typecheck and production builds passed.
- `pnpm test:agent-runtime`: **1,236 passed** (30 + 649 + 511 + 46).
- Skips: five MacTeX opt-in, one Linux-only, live Claude and live Hermes opt-in. No live model
  question, experiment or private-data change was deliberately initiated for the icon verification.

## Installed on 2026-09-14

- Source base: `303360b9b729d65bf568f79da3fbe1a137ccb67c` plus the uncommitted icon/test/docs changes.
  No new commit or push was requested for this icon task.
- Target `/Applications/GOSU.app`: **0.58.51**.
- Asar SHA-256: `aa3c50261485ad6745fe6d8149e8f4d58922329a6fa9be2b009d8d3cf68bbe0a`.
- Packaging, fixed-certificate strict app/helper checks, staging/installed version and hash checks,
  update continuity and installed isolated startup all passed. Signer is GOSU Local Code Signing,
  `093F07514506D9D61282F362792B33741C834264`. Identity was preserved and no one-time approval was
  predicted by the continuity gate; this is not a general guarantee about future OS prompts.
- The automatic menu interaction reported changed window state. The user confirmed normal Quit;
  process absence and no DB WAL were verified before backup and swap. No forced termination,
  SecurityAgent interaction, ACL/TCC change or settings reset was used.
- Previous 0.58.49 binary backup:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/GOSU-before-0.58.51-20260914-090814.app`.
- Protected state backup:
  `/Users/minsukshin/Library/Application Support/GOSU/app-backups/state-before-0.58.51-20260914-090814`.
  SQLCipher DB 3,936,256 bytes and encrypted Briefing workspace 2,366,298 bytes were byte-compared
  before launch; backup directory 0700, files 0600. Live data paths were not replaced.
- Validated staging `/Applications/.gosu-update-0.58.51-XCcjCh` was emptied by the swap and removed.
  The superseded 0.58.50 staging copy remains unused; never install it.
- Native window discovery initially timed out during startup, then succeeded after the app listener
  was available. Installed **v0.58.51**, the large chat/sparkle icon and existing Project Chat history
  were confirmed in the actual window/screenshot. No private conversation text is copied into docs.

Logs under `/private/tmp`: `gosu-large-chat-icon-focused.log`, `gosu-large-chat-icon-visual.log`,
`gosu-large-chat-icon-check.log`, `gosu-large-chat-icon-runtime.log`,
`gosu-large-chat-icon-package.log`, `gosu-large-chat-icon-sign.log`.
